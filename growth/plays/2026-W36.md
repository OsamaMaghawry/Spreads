# Growth play — 2026 W36 (Mon 31 Aug)

Context, one line: the 29 Aug seed queue is entirely unposted and `growth/log.md`
has zero entries — so this week works that backlog; nothing new is scouted or
drafted on top of it.

## The play

Clear the seed queue: the four verified threads with prepared drafts get their
quotes confirmed and go to the owner to post, one per day, value-only by default
(mention variant only where the queue already marks it). Reddit is not reachable
from this environment (see `docs/context/reachable.md`), so quote confirmation is
the owner's browser step — the drafts already carry direct thread links. The
eleven held threads wait on the owner adding `www.reddit.com` / `old.reddit.com`
to the allowlist, which lets channel-scout confirm them next week; until then no
new drafts.

## Who executes

- **the owner** — the only one who can reach Reddit this week. Open each of the
  four ready threads in `growth/queue/2026-08-29.md`, confirm the quoted line is
  still there and the thread is still open, tick `verified in browser`, then post
  one reply per day Thu 3 – Sun 6 Sep in queue order from his own account: the
  r/thetagang shopping thread (`1vyeu44`) takes value+mention as the queue marks;
  the other three default to value-only; the tasty fills thread (`1ch12q6`) only
  if it still looks alive. Tick the boxes, move each posted entry to
  `growth/log.md`.
- **reply-drafter** — one final register pass over the four ready drafts against
  `growth/playbook.md` §Claims and §Vocabulary; wording only, nothing factual;
  flag — do not fix — anything stating a product behaviour not backed by
  `docs/product-context.md` (limit walking is exit-only, and the drafts say so).
  Deadline Wed 2 Sep.
- **the owner (env)** — the durable unblock: add `www.reddit.com` and
  `old.reddit.com` to the environment allowlist so next week channel-scout can
  confirm quotes and scout `/new` itself.

## The prediction

`growth/log.md` goes from 0 to 4 posted entries by **Sun 6 Sep 2026**, and by
**Mon 14 Sep 2026** the four replies together log at least **10 engagements**
(net upvotes + direct replies + DMs, as the log format records them).

## The kill

If by **Mon 14 Sep 2026** the posted replies total **fewer than 3 engagements**
combined, or fewer than 2 of the 4 could be posted because threads were dead,
the seed-queue reply play is dropped and the next play starts from fresh
scouting — which needs the allowlist widened first.

## Hiring

None.

## Appendix (skippable)

- A Host-header route around the Reddit block was found and **rejected**: it
  circumvents the egress control the owner set, so it is not used and is recorded
  in `docs/context/reachable.md` only as a hole to close. The durable fix is the
  allowlist addition above.
- Next week, once `www.reddit.com`/`old.reddit.com` are allowed: scout pulls
  `r/options/new` and `r/thetagang/new` for fresh Theme A–E threads, and drafts
  for the verified held threads enter the next queue file.
