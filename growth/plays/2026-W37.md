# Growth play — 2026 W37 (Mon 7 Sep)

Context, one line: the reply queue (`growth/queue/2026-08-29.md`) is still
entirely unposted for a second week running — `growth/log.md` still has zero
entries — but it is blocked purely on the owner's own browser step (posting),
already has a play and a kill date (Mon 14 Sep, set last week), and per the
brief a queue "the owner alone can clear" is precisely when to look at a
different channel rather than pile a second identical play on it. No new
Reddit scouting was done and nothing new was added to the queue.

## The play

One missing GitHub secret, `SUPABASE_SERVICE_ROLE_KEY`, is silently blocking
three separate pipelines at once: `publish-blog.yml` has failed both times it
has ever run (2 and 3 Sep, "SUPABASE_SERVICE_ROLE_KEY is not set; cannot
publish"), so the two finished posts sitting on `main` since 30 Aug have
never gone live and no push has retried it since 3 Sep; `metrics-snapshot.yml`
has run successfully every day for 5 days straight but its funnel section
reads "supabase not configured" in all five files
(`docs/growth/metrics/2026-09-03.json` through `-07.json`), so signups,
live connections and paying counts are invisible; and `email-digest.yml` fails
the same way. This is a five-minute owner action — already sitting as a
`needs owner` ticket in `docs/ops/queue.md` since 2026-09-02 — that is worth
more this week than any new channel or content decision, because right now
the plan's largest channel (daily content, "a fifth to a half" of the visitor
target per `docs/growth/plan-100.md`) is producing zero live output no matter
what content-engine writes.

## Who executes

- **the owner** — set `SUPABASE_SERVICE_ROLE_KEY` as a GitHub Actions secret
  (production service-role key from the Supabase dashboard; ticket already
  filed, `docs/ops/queue.md` 2026-09-02). This single action is the whole
  unblock — no code change needed on any of the three pipelines.
- **duty-engineer or the owner** — once the secret exists, re-run
  `publish-blog.yml` via `workflow_dispatch` on `main`. It will not retrigger
  itself: the last push touching `content/blog/**` on `main` was 3 Sep, and
  nothing has pushed to those paths since. Confirm the run's conclusion is
  `success`, not just that it started.
- **content-engine** — resume the daily article cadence. Nothing has been
  committed to `content/blog/` since 2 Sep (5 days against the plan's 7
  posts/week rate); the one draft since then, `credit-spread-max-loss.md`, is
  finished but still `draft: true` on `staging` and has never reached `main`.
  Get it reviewed (seo-editor, desk-editor, compliance-gate per the normal
  daily pipeline) and, if it clears, flip `draft` and land it on `main` in the
  same push that resumes the cadence, so one `publish-blog.yml` run picks up
  everything at once.

## The prediction

By **Mon 14 Sep 2026**: `docs/growth/metrics/*.json`'s `funnel` section reads
real counts instead of `"supabase not configured"` on at least one day, and
at least 3 posts are confirmed live on the production blog (the 2 already on
`main` plus the draft, once signed off) — up from 0 confirmed live today.
Confirmation is the owner's: `dashboard.deltamint.app` and `deltamint.app`
are both 403 at CONNECT from this environment (`docs/context/reachable.md`),
so this agent cannot check production directly and did not attempt to.

## The kill

If by **Mon 14 Sep 2026** the secret still isn't set (ticket still open) or
`publish-blog.yml`'s next run still fails after it is, drop "unblock the
pipeline" as the lever and escalate it straight into the Friday board pack as
a hard blocker rather than a repeated weekly ask — the 15 Sep checkpoint's
"12 posts live in three hubs" already cannot be hit from 0 live posts today
regardless, so by 14 Sep that checkpoint is effectively missed either way and
needs the owner's decision, not another play.

## Hiring

None.

## Appendix (skippable)

- Same secret, three pipelines: `publish-blog.yml`, `metrics-snapshot.yml`
  (funnel half only — Search Console and GA4 need separate credentials, also
  `needs owner` since 2026-09-02) and `email-digest.yml` all read
  `SUPABASE_SERVICE_ROLE_KEY`. One fix, three unblocks — checked directly
  against `.github/workflows/*.yml` and `docs/ops/queue.md`, not assumed.
- Checked before writing this play, per the brief's "finished asset nobody
  shipped" instruction: `content/blog/` holds 3 files; `main` has 2
  (`options-journal-splits-spreads-into-legs.md`,
  `return-on-risk-vs-return-on-capital.md`), `staging` additionally has
  `credit-spread-max-loss.md` marked `draft: true`. `publish-blog.yml` run
  history (via the GitHub Actions API): exactly 2 runs total, both
  `conclusion: failure`, 2 and 3 Sep, neither retried since.
- The reply queue stands as last week left it: 4 drafted, verified threads
  ready to post, 0 posted, kill date Mon 14 Sep unchanged. Not re-scouted,
  not re-drafted, not this week's play — see the context line above.
- Metrics read before writing this play, per the scheduled instruction: all
  5 snapshot days (`docs/growth/metrics/2026-09-03.json` through `-07.json`,
  on `main` — `staging` carries no dated snapshots, only the README stub)
  show every source (`search`, `analytics`, `funnel`) as
  `unavailable`/`not configured`. No measured rate exists anywhere to replace
  a `docs/growth/plan-100.md` benchmark with this week; none were changed.
  A short status note was added to that file instead of fabricating a number.
