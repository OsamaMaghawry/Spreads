# Growth play — 2026 W39 (Mon 21 Sep)

Context, one line: the seed reply queue (`growth/queue/2026-08-29.md`) is still
entirely unposted — `growth/log.md` still reads "Nothing posted yet" — for a
**third** consecutive Monday (W36 asked for this, W37 pivoted to a pipeline
blocker instead of restating it, no W38 play exists), while the funnel has
been completely flat — zero net-new signups — for the same three weeks. Per
this run's instruction, backlog beats scouting: this week's play works that
queue, full stop, rather than proposing a new channel or new threads on top
of it.

## The play

Post the four fully-drafted, thread-linked replies in
`growth/queue/2026-08-29.md` — nothing left to write, only the owner's own
browser and account stand between "drafted" and "posted," and that gap has
now cost three weeks against a funnel that has produced exactly zero net-new
signups (`signedUpLast7: 0` in every `docs/growth/metrics/*.json` snapshot
from 2026-09-08 through today) in the meantime. This week narrows the ask to
the single lowest-friction version: the r/thetagang shopping thread
(`1vyeu44`) first, since it explicitly asks "what's the community consensus"
and already carries the value+mention draft the queue marks as default there
— then the other three on the existing one-per-day cadence.

## Who executes

- **the owner** — open `growth/queue/2026-08-29.md`, start with the
  r/thetagang shopping thread, confirm it's still open and the quote still
  matches, post the value+mention draft as-is or lightly in your own words,
  tick the boxes, move the entry to `growth/log.md`. Repeat for the other
  three (r/options spreadsheet-fatigue x2, r/thetagang fills) one per day
  through Thu 24 Sep. All four drafts already passed a register check in the
  W36 run; nothing new to review before posting.
- **reply-drafter** — one five-minute pass over the four drafts against
  `growth/playbook.md` §Register before the owner posts: the "trader
  explaining to another trader" phrasing this section was written against was
  withdrawn 9 Sep (after these drafts were written, 29 Aug) for running long
  and literary. Flag only — do not rewrite — any sentence that reads that way;
  the owner edits at posting time regardless, so this is a safety check, not
  a blocker. Deadline Tue 22 Sep, ahead of the first post.
- **the owner (env)**, unchanged ask from W36: `www.reddit.com` /
  `old.reddit.com` stay allowlisted for reads but anonymous access is still
  refused by Reddit itself and agent-side credentials remain unavailable
  (`docs/context/reachable.md` §"Reddit API" — not re-raised, per that
  section's own instruction not to ask again).

## The prediction

`growth/log.md` goes from 0 to at least 2 posted entries by **Fri 25 Sep
2026** (half the original W36 target, since three weeks of zero is the more
urgent signal now, not full completion). By **Mon 5 Oct 2026** (two weeks),
the posted replies combined log at least 5 engagements (net upvotes + direct
replies + DMs) **or** at least 1 signup lands with `signup_source=reddit`,
visible in `docs/growth/metrics/*.json`'s `bySource`, which today shows only
`"unknown": 4` for every signup — no channel has ever been attributed.

## The kill

If by **Mon 5 Oct 2026** fewer than 2 of the 4 replies have been posted at
all — meaning the browser step still hasn't happened a third time running —
drop Reddit replies as a channel entirely, not just this batch, and escalate
the owner-posting bottleneck itself to the Friday board pack as a structural
blocker: the content channel (13 posts live, see appendix) does not have this
problem and does not need the owner's own account to move, so a channel that
categorically requires it may not be one this org can run on a weekly
cadence. If instead 2+ get posted but combined engagement stays under 5 and
no `reddit`-sourced signup appears, keep the channel but drop these four
threads and this wording, per the plan's own kill rule (`docs/growth/plan-100.md`:
"under 2 signups from 10 replies → change the threads, not the replies").

## Hiring

None.

## Appendix (skippable)

- **The funnel has not moved in three weeks.** Every `docs/growth/metrics/*.json`
  from 2026-09-08 to 2026-09-21 (14 consecutive daily snapshots, checked on
  `origin/main` since this branch's own copies stop at 2026-09-14 — see next
  bullet) reads `signedUp: 4`, `signedUpLast7: 0`. `tradedLive` moved once,
  1→2, on 2026-09-13, and has been flat since. `paying: 0` throughout. This is
  the headline number for the board pack, not something this play can fix on
  its own — it is named here because it is the reason this week narrows the
  ask rather than repeating it unchanged a third time.
- **This branch's own metrics stop at 2026-09-14**; `origin/staging`, which
  this run was started from per instruction, has not received a
  metrics-snapshot commit since then, while `origin/main` and the
  `claude/zealous-hypatia-n8rd4l` branch carry daily snapshots through
  2026-09-21 (identical content on the 21st, `signedUp: 4`,
  `signedUpLast7: 0`). Read directly via `git show origin/main:...` for this
  play rather than assumed; `docs/growth/plan-100.md` is revised below using
  the 2026-09-21 numbers since they are the genuinely newest data reachable,
  but this branch gap (staging a week stale against main) is itself worth the
  owner's or duty-engineer's attention — not raised as this week's play
  because it is not a growth channel.
- **Search Console and GA4 remain unconfigured** — `docs/ops/queue.md`,
  needs-owner ticket open since 2026-09-02, now 19 days. This is why the
  visitor→signup benchmark below could not be replaced with a measured rate:
  there is no visitor count anywhere to divide by.
- **Billing is still not live in production** — the Stripe ticket
  (`docs/ops/queue.md`, 2026-09-02) is still open. `paying: 0` therefore
  reflects "no way to pay yet," per `plan-100.md`'s own caveat, not a
  conversion failure — the live→paid benchmark below is left unchanged for
  this reason, not because no data exists.
- **Content is not this week's problem.** 13 posts live on `main`
  (`content/blog/`) as of today, against the plan's 7/week rate and ahead of
  the 15 Sep checkpoint's "12 posts live" bar. No action asked of
  content-engine or seo-editor this week.
- **No 2026-W38 play exists** anywhere reachable (checked `origin/main`,
  `origin/staging`, this branch) — last Monday's run either didn't happen or
  wasn't committed. Flagged, not investigated; not this agent's channel to
  chase down.
- Guest appearances/pitches (plan's fourth channel, 1 pitch/week) have no
  tracking surface anywhere in the repo and no evidence any pitch has been
  sent since the plan was written. Not this week's play (backlog-first), but
  worth a line in the board pack: three of four plan channels currently have
  zero recorded activity (Reddit unposted, guest pitches untracked, paid
  search unfunded) and only content is moving.
