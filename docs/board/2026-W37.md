# Board pack — 2026-W37 (week of Mon 7 Sep, Friday 11 Sep)

## State of the business

The week's one real event: the `dumpBrokerFeed` credential hole — any caller
holding the public app key could decrypt any account's broker credentials —
is now closed in **production**, not just staging, along with a live
order-safety race and three days of other staged fixes (`0ac2e27`,
`e32a8a7`, `ad3602a`). Everything else is stuck exactly where last week left
it: zero blog posts reached production this week despite four being fully
drafted, zero Reddit replies posted (kill date is Monday), the metrics panel
still reads mostly blank, and the org now has **20 unmerged `claude/*`
branches** — several independently duplicating the same blog post — instead
of last week's nine. The production Supabase host that every weekly report's
email depends on has been 403 for the whole week; this pack's own email is
expected to fail the same way (see decision 1).

## The numbers against the plan

Target: **100 paying users by 31 Dec 2026** (`docs/growth/plan-100.md`).

| Metric | This week | Plan says |
| --- | --- | --- |
| Visitors | **not connected** — GA4 not configured | ~760/day needed |
| Signups | **4 total, 0 new this week** | ~2,700 total needed |
| Live connections | **4** (1 has traded live) | ~670 total needed |
| Paying | **0** | 100 |

`docs/growth/metrics/README.md`: Search Console and GA4 both still "not
configured." The funnel is at least reading real numbers now (was blank two
weeks ago) — it just hasn't moved in 4 days.

Channels against their kill numbers (`plan-100.md`):

| Channel | This week | Kill number |
| --- | --- | --- |
| Daily articles | 4 posts fully drafted (bid-ask spread, call-vs-put ×2, options-contract) across **4 different branches** — **0 reached production**; content-merge automation has never once fired successfully | flat impressions after 20 posts — still too early, but 0 live in 9 days is its own problem |
| Reply queue (Reddit) | **0 posted**, still 4 ready-to-go drafts, unchanged two weeks running | kill check is **this Monday, 14 Sep**: <2 signups from 10 replies |
| Guest pitches | 0 sent, no evidence a pitch has ever gone out | none accepted in 4 weeks → drop |
| Paid search | not started | CPS > $15 → pause |

## What shipped this week

Plain English, from `docs/ops/shipped.md`:

- **A real security hole is closed in production.** Anyone holding the
  app's public key could previously name any account and pull its decrypted
  broker credentials and full history. That's fixed now, live — not just on
  staging where it sat for two days.
- **The Stop button race is fixed.** Hitting Stop on a multi-order close
  could still let one more order fire; it can't now.
- A stuck resting order can no longer resubmit as a broken null-symbol order
  when repricing.
- **The Broker tab is live:** see exactly what Alpaca reports you hold, with
  safety-ordered multi-close (buy-backs always before sales).
- **A missing quote can no longer price a close at half its real value** —
  the SPY-shares bug that showed a $373 mid on a $746 bid is fixed.
- The scanner can now scan the **whole listed market**, not just the S&P 500.
- Single-leg wheel orders (cash-secured puts, covered calls) place directly.
- EU/UK/Switzerland visitors now get analytics consent-gated by geography.

Full ledger: `docs/ops/shipped.md`.

## Department headlines

**Ops (duty-engineer, all week):** closed the one finding that actually
mattered — the broker-credential hole — in production, three days after
fixing it on staging. Everything else this week was maintenance.

**Trading (head-of-trading, Thursday — `docs/trading-audit/2026-W37.md`,
branch `claude/awesome-ritchie-6jhuiu`):** two new, executed defects, no
STOP. A short call covered by a *long call* struck below it (the common way
to build a poor-man's-covered-call) shows two disagreeing risk numbers on
one screen — the account's own "Max risk" tile is correct, but the
Detailed-table total is off by exactly the short's collected premium
($226 on the example used, scales with size) — and the same position's
break-even reads **-$2.26**, a stock price that cannot exist. Also: **last
week's risk-understatement finding is still unfixed, seven days later, and
had never been ticketed until this audit filed one.**

**Product (vp-product, Tuesday):** caught and fixed its own file lying —
`pricing.md` had said "no billing exists" for five days after Stripe
shipped. Backlog unchanged, three open proposals, second week running
nothing shipped against them. $29/mo·$290/yr two-tier pricing confirmed
holding.

**Branding (head-of-branding, biweekly — `docs/branding/2026-W37.md`,
branch `claude/ecstatic-mccarthy-oukfeq`):** **none of last cycle's top five
fixes were applied** — same two-week-old list, unchanged. New this cycle:
the homepage's own meta description and hero copy — what Google and every
social share shows — call the core feature "a scanner," while the app, the
pricing page and the homepage's own mockup three lines later all say
"Screener."

**Content (Head of Content):** four posts fully drafted this week, and the
reality check found the actual cause of zero landing: `content-merge.yml`'s
allowlist rejects any post that ships with its own diagrams as "not
content-only," so an illustrated post silently sits forever with nobody
merging it. Separately confirmed: this automation has **never once**
successfully auto-merged a post in its history — every post now live got
there by a manual merge.

**Growth (vp-growth, Monday — `growth/plays/2026-W37.md`):** correctly
declined to add a new channel and instead named the play "get the one
missing secret set" — `SUPABASE_SERVICE_ROLE_KEY`, blocking blog publishing,
the metrics funnel and the email digest at once. The owner set it Sunday.
The funnel now reads real numbers; the blog still doesn't publish, because
the actual blocker (the merge-gate allowlist above) is a different bug than
the one the play named.

**SEO (seo-editor, Wednesday):** added 10 more keyword rows; re-verified
reachability; its own growth-report email failed the same way every other
weekly email did this week (`docs/context/reachable.md`).

## Decisions needed from Osama

1. **The production Supabase host (`yecfbeohyakuoyczvdbj.supabase.co`) has
   been unreachable from every agent session all week**, taking down the
   Wednesday SEO report email, last week's board pack email, this week's
   Monday reality-check email, and — expected — this pack's own email.
   Either allowlist the host for agent sessions, or move every weekly report
   onto the GitHub Actions email path the way `email-digest.yml` already
   works. Until one happens, you only learn any of this exists by opening
   the repo. (`docs/ops/queue.md`, 2026-09-09)
2. **Turn off the Cloudflare-side Workers Build** for `spreads` /
   `spreads-staging` (Settings → Builds). It's still running redundant to
   the new GitHub deploy and was caught overwriting the *production* worker
   with a *staging* bundle twenty seconds after a staging deploy — one
   "Deploy version" click from serving staging to real users. (2026-09-08)
3. **Say whether agent-manager may merge docs-only weekly branches.** Last
   week this was nine stray branches; this week it's **twenty**, several
   holding independent duplicate drafts of the same blog post. Nobody reads
   nine branches to find the trading audit; nobody will read twenty.
4. **The blog's auto-merge pipeline has never once worked and needs a real
   fix, not another week of manual merges.** `content-merge.yml`'s allowlist
   doesn't cover a post's own diagram files — add
   `landing/public/assets/blog/**` to it — or every illustrated post keeps
   stalling silently forever.
5. **A re-audit is required before the leg-first risk-engine rebuild goes to
   production.** Every blocking finding from the last bench review is fixed,
   but the bench hasn't re-read the result as a whole yet — say whether that
   re-audit happens before or after this pack, since nothing on that branch
   should ship without it. (`docs/ops/queue.md`, 2026-09-08)
6. **Stripe test-mode setup** — create "DeltaMint Live" with monthly/annual
   prices, set the staging secrets, add the webhook. Still blocks billing
   from being testable at all. (2026-09-02)
7. **Decide who the watch emails** — the subscriber, or only your inbox.
   Blocks "alerts" from appearing on the pricing page. (`pricing.md` §6)
8. **Research allowlist** — `tiblio.com`, the `optionstrat.com` apex,
   `optionalpha.com`, `quantwheel.com`, `puthouse.com`, `wingmantracker.com`
   are all still blocked at the network policy level. Every competitor price
   cited anywhere is still "reported," not "verified," until this opens.
   (2026-09-02)
9. **GA4 + Search Console setup** — nothing populates the metrics panel
   above until this exists. (2026-09-02)
10. **`OPS_TOKEN` / `DELTAMINT_OPS_TOKEN` still not set** — duty-engineer
    still can't read order errors or alerts; every hourly run is
    code-and-site checks only. (2026-09-02)
11. **Options Wheel staging account has no `wheel_client_prefix`** — its
    adjusted basis reads "broker basis" instead of the real one. (2026-09-02)
12. **`228fea9` (the scanner's market-hours fix) has been sitting on
    `staging` since 4 Sep with no PR to `main`.** Say if it was just missed
    or is being held back on purpose. (2026-09-07)

No hiring proposal this week. No live STOP finding this week — the trading
audit returned FINDINGS, not STOP (decision 5 above is about the *prior*
STOP's fixes, not a new one).

## What changed since last week

- **Fixed since last week's pack:** the `dumpBrokerFeed` credential hole,
  now closed in production (was staging-only); `SUPABASE_SERVICE_ROLE_KEY`
  and its staging twin, both set — unblocking the first digest email this
  repo has ever sent and the funnel numbers now reading real counts;
  migration `0024` confirmed applied to production; the EU/UK consent
  question — owner decided "do it," geo-gate shipped.
- **Same pricing intent as W36, now confirmed rather than disputed:**
  $29/mo · $290/yr, two tiers, Paper free — the $19 alternative is
  explicitly closed out, not still open.
- **Worse since last week:** unmerged branches, 9 → 20. Zero blog posts
  live, same as last week, despite four more finished drafts. Last week's
  trading-audit risk-understatement finding is now confirmed still unfixed
  at one week old, with its first ticket only just filed.

## What nobody got to

- The pricing page's Paper-tier copy is stale again — it doesn't mention
  cash-secured puts/covered calls or the new whole-market scan, both live
  since Wednesday. Filed as a ticket for next Tuesday's cadence.
- No guest pitch has gone out in three weeks of the play existing.
- Nobody has re-examined whether last week's risk-understatement bug also
  reaches iron condors or single-leg positions — still open.
- `positionWatch` has never been run against the new ratio-spread shape —
  queued since 2026-09-08, still untouched.
