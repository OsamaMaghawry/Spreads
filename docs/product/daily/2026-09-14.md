# 2026-09-14 — end of day

- **Shipped:** the day's one real product event was an outage and its fix,
  not a new capability: the close ticket read its own quantity before the
  value existed (a temporal-dead-zone bug in `CloseDialog`) and crashed to a
  blank white page on every mount, live in production since this morning's
  order-card release — closing a position, the one action that must always
  work, didn't. Fixed by reordering the quantity block, given the app's
  first error boundary anywhere, and backed by a new lint rule
  (`no-use-before-define`) so the same class of bug fails lint instead of
  reaching a trader (`c576da7`, `b9c2555`, merged to `main` in `c835e0f`).
  `docs/ops/shipped.md` was missing this line on `main` — a duty-engineer
  session had already written it on `staging`; added the same entry here.
  Everything else logged as "2026-09-14" in `shipped.md` (the Orders tab
  rebuild, the Scanner rename, security headers, the saved-ticket risk fix)
  is duty-engineer's same-day catch-up of older, already-shipped work, not
  new activity from today.
- **Backlog:** unchanged. Nothing that shipped today touches any of the
  three open proposals — quoted exit width, POP ranking, or recording what
  was scanned (`scanEntries`/`findEntry` still insert nothing; checked
  directly). Still three of five slots used, last ranked 2026-09-01; that
  ranking is Tuesday's job, not today's.
- **Positioning:** unchanged. Today's fix restores an existing claim
  (closing always works) rather than adding a new one — nothing in
  `docs/context/positioning.md`'s competitor comparison or
  `docs/product/features.md`'s inventory turns on whether `CloseDialog`
  had a dead-zone bug this afternoon. The Scanner rename and Orders-tab
  work duty-engineer catalogued today are both already reflected on the
  live landing page (checked: it already says "Strategy Scanner", not
  "Screener").
- **The page:** no ticket needed. Checked both `landing/public/index.html`
  and `landing/public/pricing/index.html` for anything today's shipments
  would make false or would newly omit — the Scanner naming is already
  correct there, and neither page makes a claim about the close ticket's
  reliability that today's crash (or its fix) bears on.
- **Not gotten to:** did not re-audit the Orders tab / saved-ticket /
  security-header entries duty-engineer added to `shipped.md` today for
  positioning or feature-table impact, since none of that code changed
  today — that reconciliation belongs to Tuesday's full pass, not a spot
  check of a same-day catch-up. Migration `0053`'s production-apply status
  (flagged `needs owner` in the queue today) is outside this question and
  wasn't touched.
