# 2026-09-17 — end of day

- **Shipped:** four related changes reached `main` today, all tracing to one owner complaint ("2.4k+ is not justifiable in anywhere") about Alton's week-of-7-September figures: Account Analysis's chart now draws the close a window is measured *from*, with a new panel breaking the window's total into the same four parts the weekly digest already emails (`33b1333`/`e8787b0`); the chart and the digest now share one baseline and one "gap to broker" explanation instead of contradicting each other (`f6d610c`/`0dd5122`); a spread's long leg blanking 52 days of history because it sat nested under its parent order's symbol is fixed, and the digest now keeps a permanent record of what it actually sent (`a1127e8`/`7386f6b`, `ec6b0f3`/`bf9180f`, migration `0054`); and stored history is now frozen against being silently rewritten by a future rebuild (`7da283d`/`294175c`, `22c5b44`/`a9dbf8b`). `docs/ops/shipped.md` on `main` was missing all four lines (they were only on `staging`'s copy) — added, main hashes cited. Two more commits landed today (Supabase CLI auth for deploy workflows, the metrics snapshot) — infra/automation, not user-facing, not logged here.
- **Backlog:** unchanged. None of today's four touch the three open proposals (quoted exit width, POP ranking, scan-run recording) — all four are correctness fixes to an already-shipped, already-PAID surface (Analysis, the digest), not new scanner capability. Still three of five slots used, last ranked 2026-09-01 — that rank is now over two weeks stale; flagging for Tuesday, not fixing today.
- **Positioning:** unchanged, but `docs/product/features.md`'s Analysis table got a new row (A11) for the window-breakdown panel (`WindowParts.jsx`) — a genuinely new user-visible element, not just a bug fix, so the inventory needed it even though no claim in `docs/context/positioning.md` moved. No competitor comparison is affected; these are internal accuracy fixes, not a new capability versus Tiblio/Barchart/etc.
- **The page:** no ticket. Neither `landing/public/index.html`'s Analysis sample section nor the pricing page claims anything about window measurement or digest/Analysis agreement that today's fixes contradict or newly support. Note: the pricing page does not exist at `landing/public/pricing/index.html` as instructed — it's currently a draft at `landing/drafts/pricing/index.html`, not yet published; read that instead and found nothing today's shipments change there either.
- **Not gotten to:** migration `0054`'s production-confirmation deadline (Saturday 13:00 UTC, or the weekly digest fails to send to everyone) is already open in `docs/ops/queue.md` as `[needs owner]` and was already emailed by duty-engineer today — not duplicated here, since it's a migration-state question, not a should-this-exist one. The backlog's 2026-09-01 staleness (above) also wasn't acted on; that's Tuesday's job.
