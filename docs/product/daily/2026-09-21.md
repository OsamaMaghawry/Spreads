# 2026-09-21 — end of day

- **Shipped:** one change reached `main` today, and it's content, not product: a tenth foundations post, "Option assignment: what happens in your account," went live — the two-stage OCC/clearing-firm allocation, a short-put-vs-short-call side-by-side, and a worked example walking the option row disappearing and a stock row replacing it line by line. Full review chain ran (desk-editor: three blocking mechanics/arithmetic fixes; seo-editor; compliance-gate: no findings) before the content gate auto-merged it straight to `main` (`b9c8589`, `816669a`, `06f6345`, `8217143`, `59a5852`). `docs/growth/metrics/2026-09-21.json`'s snapshot commit (`3e5c4cd`) is CI housekeeping, not a shipment. `docs/ops/shipped.md` was missing the post's line — added. Note for the record: `main`'s copy of `shipped.md` is a week stale against `staging`'s (which already carries six real entries from 09-17 through 09-20 — SnapTrade and Tradier broker evaluations, Analysis grouping fixes, an internal-mail routing change — none merged to `main` yet); that gap is duty-engineer's ledger to close, not reconciled here.
- **Backlog:** unchanged. Today's only shipment is a compliance-reviewed blog post that makes "no product claims" by its own review note — it doesn't touch any of the three open proposals (quoted exit width, POP ranking, scan recording) or the killed universe-instrumentation item. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** unchanged. A foundations article carries no product claim to true up or retract, so nothing in `docs/context/positioning.md` or `docs/product/features.md` changes on today's evidence.
- **The page:** no ticket needed. Neither `landing/public/index.html` nor `landing/public/pricing/index.html` names a post count or the foundations series, so today's tenth post doesn't make either page's copy stale.
- **Not gotten to:** the SnapTrade/Tradier broker-evaluation work and the Account Analysis grouping fixes sitting on `staging` since 09-17–09-20 were read for context (org-required staging diff) but are outside today's question — they haven't reached `main`, so they're not "shipped" by this file's own definition and aren't reconciled against the backlog here. They're worth a look whichever day they merge.
