# 2026-09-16 — end of day

- **Shipped:** nothing reached `main` in the last 26 hours except a metrics
  snapshot (`12f1647`); `staging` saw only clean-hour duty-engineer runs, no
  new code. Reconciling `docs/ops/shipped.md` against `main`'s actual commit
  history found two stale lines from `2026-09-14`, both left over from before
  the `c835e0f` merge: the saved-ticket reopen fix and the eighth foundations
  post (implied volatility) were both tagged "(staging)" / "not yet merged to
  production `main`" when both had in fact reached `main` — and, for the
  post, published live — the same day. Corrected both, and added the missing
  line for `c835e0f` itself (the close-ticket crash / error-boundary fix),
  which had no shipped.md entry on `main` at all.
- **Backlog:** unchanged. None of the three open proposals (quoted exit
  width, POP ranking, `scan_runs` recording) shipped or half-shipped —
  `ResultsTable.jsx`'s "Width" column is still wing width, not the bid/ask
  quoted width #1 asks for; no POP field or sort exists; no `scan_runs` table
  exists in `supabase/migrations/`. Still three of five slots used, last
  ranked 2026-09-01; this is a reconciliation pass, not the Tuesday run, so
  no re-ranking or new proposals.
- **Positioning:** unchanged. Nothing shipped today to check a claim against,
  and today's shipped.md corrections describe code that already reached
  `main` two days ago — `docs/context/positioning.md` and
  `docs/product/features.md` were not touched.
- **The page:** no ticket. `landing/public/pricing/index.html`, the path the
  daily brief names, does not exist yet — pricing is still
  `landing/drafts/pricing/index.html`, unpublished. Read it against
  `features.md` and today's (non-)shipments anyway: no claim in it needs a
  wording change from anything that happened today.
- **Not gotten to:** the open `docs/ops/queue.md` item on production's
  unverified migration state (`0051`–`0053`) is a production-database
  question for the owner, not a product one — noted, not touched. The
  `vp-product` brief itself (`.claude/agents/vp-product.md`) still points
  research at `src/pages/Screener.jsx`, which no longer exists post-rename
  (now `src/pages/Scanner.jsx`/`src/components/scanner/`) — worth a fix
  before the next Tuesday run, not done here since this pass doesn't touch
  agent briefs.
