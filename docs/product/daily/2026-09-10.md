# 2026-09-10 — end of day

- **Shipped:** `main` carries two ships from late 9 Sep, neither logged until now. `0ac2e27` put everything staged since 2 Sep into production in one merge — auto cancel-and-resubmit on a stuck reprice, the quote-sanity fix that stops a missing bid/ask from pricing a close at half its value, the Broker tab and its safety-ordered multi-close, a fourth screener universe (the whole listed market, not just the S&P 500), single-leg wheel orders (cash-secured put / covered call) opening as plain option orders, EEA/UK/Switzerland analytics consent-gating, and — the one that mattered most — the `dumpBrokerFeed` credential-exposure hole is now closed **in production**, not just staging (any caller with the public project key could previously pull any account's decrypted broker credentials and full history). `ad3602a`/`e32a8a7` then fixed a live Stop-button race on the multi-close (pressing Stop could still let one more order fire) plus a reprice bug that could resubmit a cancelled spread's parent as a null-symbol single-leg order. `docs/ops/shipped.md` had no lines for either — added. `origin/staging` is 21 commits further ahead (equity-curve rebuild, Whole/Premium view, strategy comparison, OpenBookPanel) — none of it on `main` yet, so none of it counts as shipped today.
- **Backlog:** unchanged. None of the three open proposals — quoted-width exit cost, POP ranking, scan-run recording — shipped or half-shipped; today's production work was order-path safety, a security fix, and screener/execution surface, not the ranking, columns or logging those three proposals are about. `docs/product/backlog.md` stands as vp-product left it 2026-09-01.
- **Positioning:** `docs/context/positioning.md` needed no change — nothing shipped today adds a broker, closes a named competitor gap, or moves an anchor price. `docs/product/features.md` did: S3 (wheel-scan strategies) and T1 (single-leg wheel order) were still tagged `staging` from 2 Sep; both are `main` as of `0ac2e27`. S2's universe row and the Back-office `dumpBrokerFeed` line updated to match — the latter now also notes the credential hole is closed on `main`, not just staging.
- **The page:** `pricing/index.html`'s Paper-tier line ("Screener — put spreads, call spreads and iron condors across the S&P 500 or your own list") is now stale — it omits cash-secured puts/covered calls (free on Paper, per S3) and the new whole-market universe option, both live on `main` as of tonight. Exact wording filed as an `open` ticket in `docs/ops/queue.md` rather than edited directly.
- **Not gotten to:** no email sent, though the page ticket above would normally trigger one. `.claude/agents/vp-product.md` has no "Tuesday prompt" section and no `sendDigest` curl recipe (checked directly — same gap the 2026-09-02 run hit), and `scripts/email-digest.mjs`'s own header states scheduled/routine sessions run without the service-role credential `sendDigest` needs by design; this session has neither `SUPABASE_URL` nor `SUPABASE_SERVICE_ROLE_KEY` set. Also unverified: whether the `docs/ops/queue.md` escalation dated 2026-09-09 ("`dumpBrokerFeed` is unauthenticated in production") should now read `[fixed]` given tonight's merge — left for duty-engineer's own ledger rather than edited here, outside this run's file ownership.
