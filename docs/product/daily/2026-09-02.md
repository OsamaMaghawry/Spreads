# Daily product reconciliation — 2026-09-02

vp-product, end-of-day pass. `main` at `e672753`; `staging` 38 commits ahead
(billing, blog/growth loop, ops tooling — none of it merged to `main` yet).

**What shipped (to `main` today):** the naked-call watch stopped flagging a
short call covered by a long call as naked (`616c203`/`c680972`, PR #3,
cherry-picked ahead of the rest of staging); `positionWatch` no longer
crashes every account on `sharesByTicker` (`a7db799`); wheel positions carry
an adjusted cost basis and single-leg positions (CSPs, covered calls,
assigned shares) render as what they are, with undefined risk on a naked
call (`e1fc417`, `b84515d`); stock-like risk is sized at a 15% adverse move
with stock-to-zero broken out as Notional (`9217869`); price walking now
works on the open as well as the close, with an owner-set floor
(`6f6f0a5`); the close ticket's price slider and P/L follow a dragged price
(`3a22207`); the after-close report reads as headline / needs-a-look /
everything-else (`f1515e4`); the dashboard replaced its 60-second poll with
a shared streaming connection and continuous background refresh
(`2669b2f`, fixed by `dc26117`); an Orders tab now shows every working and
today-ended order grouped as sent, with per-leg fills and reject reasons
(`00aa175`); `syncAccounts` stopped throwing "admin is not defined" on
every account (`ef55fa5`). Two of these — `00aa175` and `ef55fa5` — reached
`main` but have no line in `docs/ops/shipped.md` (that ledger currently
lives only on `staging`, pending its own merge); flagging for duty-engineer
rather than forking the file onto this branch.

**Backlog:** no change. None of the three open proposals (quoted-width exit
cost, POP column, scan-run recording) shipped or half-shipped today — the
day's commits were watch-accuracy, position-typing, risk-sizing and
streaming fixes, none of which touch the screener's ranking, columns or
logging. `docs/product/backlog.md` stands as vp-product left it 2026-09-01.

**Positioning:** no change. Nothing that shipped today adds a broker,
closes a competitor gap, or moves an anchor price; `docs/context/positioning.md`
and `docs/product/features.md` (rewritten today, commit `67de69b`, already
current through `3a22207`) both already describe today's state accurately —
including the one commit after that write, `c680972`, which only makes the
naked-call rule's existing claim more accurate, not different.

**The page:** `landing/public/pricing/index.html` (production) still
misstates the product, unchanged from the state `docs/product/pricing.md`
§1 already documented this morning. Ticket, ready to copy into
`docs/ops/queue.md` (not created here — that file also lives only on
`staging`):
`[open] 2026-09-02 · vp-product · Pricing page claims Pro-only "60-second
auto-refresh" (refresh is continuous now, P3/dc26117); iron condors and
single-leg closing gated to Pro/Desk (both free on every account, S1/T11/T12);
master risk aggregation gated to Desk (free, P1); PDF export gated to Desk
only (paid-tier generic, not Desk-specific, A10); account/position caps of
1/3/unlimited and 5/unlimited (no plan column or billing exists yet to
enforce any cap, pricing.md §1). Replacement copy already drafted in
pricing.md §2 (two-tier Paper/Live); owner decision needed per pricing.md §6
item 1 — replace at the Stripe switch or cut to one line now. Evidence:
features.md rows S1, T11, T12, P1, P3, A10; pricing.md §1, §6.`

**What nobody got to:** the sendDigest curl recipe this run was told to use
("`.claude/agents/vp-product.md`'s Tuesday prompt") does not exist — that
file has no Tuesday-prompt section and no curl recipe, and `scripts/email-digest.mjs`'s
own header states routine/scheduled agent sessions run without the
service-role credential sendDigest requires, by design, so no email was
sent despite the page-misstates-the-product trigger firing. Also unresolved:
`docs/ops/shipped.md` and `docs/ops/queue.md` exist only on `staging`, not
`main`, so today's reconciliation could read but not write either — both the
missing shipped-ledger lines and the pricing-page ticket above need a human
or duty-engineer to land them on the right branch.
