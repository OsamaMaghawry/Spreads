# 2026-09-11 — end of day

- **Shipped:** nothing reached `main` today — four substantive commits landed on `staging` only: Account Analysis now counts open option positions (not just closed trades and shares) in the headline, return on equity/risk and a new panel, marked at today's broker price and labeled unrealized (`0cde265`, corrected in `a49da5e` for a stale illustration in the commit description, not the fix itself); a batch of daily-equity-chart bug fixes including one that could store a live option book as a false $0.00 day (`9faf020`); and Trade History now syncs hourly on a schedule for every connected account instead of only when someone opens the page, closing a gap where an unopened live account sat 24 trades stale (`b6d26bf`). `docs/ops/shipped.md` already carries all of these with the `(staging)` tag — duty-engineer logged them this morning (`c7d6a65`); nothing missing, nothing added here.
- **Backlog:** unchanged. All three open proposals (quoted exit width, POP ranking, scan recording) live in the screener (`optionScan.ts`, `scanEntries`, `findEntry`); today's work touched none of it — it's Account Analysis, the equity table and trade sync. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** unchanged. Nothing shipped to `main`, so nothing we can now honestly claim changed and no competitor comparison moved. Flagging for the Tuesday run: once the open-positions and scheduled-sync work reaches `main`, `docs/product/features.md` rows A6 ("Equity curve of realised P/L") and H2 ("Self-syncing, 15-minute staleness") will both be stale against the code and need rewriting, and Analysis will need a new row for open-position coverage that doesn't exist in the table at all today.
- **The page:** no ticket. Neither `landing/public/pricing/index.html` nor `landing/public/index.html` makes a claim about open-position coverage or sync scheduling that today's staging-only work would contradict or newly support.
- **Not gotten to:** duty-engineer deliberately opened no PR to `main` this run — `staging` is dozens of commits ahead (leg-first rebuild, billing, the Broker tab, the daily-equity feature and more), and bundling it all into one release PR is the owner's release-gate call, not duty-engineer's or mine to force (`f674e38`). That backlog of unmerged, unreleased work is the real state of the product right now and is worth the owner's attention on its own terms, separate from today's reconciliation.
