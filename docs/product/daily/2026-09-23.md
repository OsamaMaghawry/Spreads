# 2026-09-23 — end of day

- **Shipped:** three user-facing changes reached `main` today. (1) The logo is
  now the lowercase Greek delta itself, drawn from the wordmark's own font
  instead of the old triangle-with-two-sprigs mark, with its size, gap and
  vertical centering against the wordmark derived from one number instead of
  four independent hand-set ones — corrected twice more on the owner's eye,
  and the app and marketing site now share one wordmark casing for the first
  time (`386841d`, `9b67a8e`, `507dc15`, `2858a90`). (2) A twelfth foundations
  post — options expiration mechanics — is live on the blog (`63ca49e`). (3)
  Unfinished broker work (SnapTrade/Tradier) is now excluded from the
  production bundle by a build-time flag rather than a branch, so it can no
  longer hold small fixes hostage behind it (`4fc84f3`, reverted `c655223`,
  `e87c119`) — internal, not user-visible, but it changes how future releases
  reach `main`. `docs/ops/shipped.md` was missing all three lines — added.
  `staging` also carries four days' worth of covered-call/option-chain fixes
  and a reworked (lab-flagged) Analysis layout that have not reached `main`
  yet — not counted as shipped today.
- **Backlog:** unchanged. None of today's three main-shipped changes touch
  the three open proposals (quoted exit width, POP ranking, scan recording) —
  brand, content and build tooling, not scanner capability. Still three of
  five slots used, last ranked 2026-09-01.
- **Positioning:** unchanged. A logo redraw and a wordmark unification don't
  change any claim in `docs/context/positioning.md` or any row in
  `docs/product/features.md` — nothing about what the product does moved
  today, only what it looks like.
- **The page:** no ticket needed, and one thing worth flagging for next time —
  `landing/public/pricing/index.html` does not exist; there is no live pricing
  page, only `landing/drafts/pricing/index.html` (pricing/billing is still
  gated off, per `docs/ops/queue.md`). The draft already picked up the new
  wordmark in the same commits that touched the home page, so it isn't
  drifting from the brand change; `landing/public/index.html` was checked for
  any post-count or feature claim today's blog post or brand change would
  make stale and none was found.
- **Not gotten to:** the staging-only covered-call/option-chain work and the
  lab-flagged Analysis rebuild weren't reconciled against the backlog or
  positioning — by design, since neither has reached `main`; worth a look the
  day they do. Also unresolved from the standing queue: the blog-diagrams
  deploy gap (three more posts now affected per staging's ledger) and the
  `agents@deltamint.app` Brevo sender confirmation — both outside this run's
  question.
