# Daily product reconciliation — 2026-09-03

Shipped today: the wheel scanner's second half (cash-secured puts, covered calls, a Wheel scan, single-leg orders under the wheel prefix) and yesterday's order-ticket/billing/blog work reached `main` via `fbed254`; a draft-preview link and the `billing_visible` kill switch landed directly; the landing site got a real CI deploy path, Google Analytics, and Hotjar session replay, all verified live in production — `shipped.md` had four of these still marked `(staging)` after they'd already merged, and two (Hotjar, the compliance wording fix on the privacy page) missing entirely; all six lines added or corrected.

Backlog: no change. None of today's shipments touch the three open proposals (quoted-width exit cost, POP column, scan recording) — the wheel-scanner work landed in the same files as proposal #1's evidence (`optionScan.ts`, `ResultsTable.jsx`, `scanEntries`) but added delta-band enforcement and single-leg pricing, not a width/liquidity column or a scan log, so none of the three change status.

Positioning: no change. `positioning.md` makes no claim that depended on the wheel scanner's staging/main status, and none of today's other ships (analytics tags, billing switch, blog preview) touch a competitor comparison. `features.md` did need a fix and got one: S3 and T1 were tagged `staging`; both are `main` as of `fbed254`, table updated with a dated note rather than a full re-walk.

The page: nothing to queue. `pricing/index.html` already claims cash-secured puts and covered calls (added in the same `fbed254` merge that shipped the code), so the claim and the code agree now that both are on `main`; no marketing page asserts anything about analytics/session-recording that the new Hotjar/GA tags would contradict.

Didn't get to: whether the wheel scanner's arrival on `main` changes the pricing tiers or the Alpaca fee schedule math in `pricing.md` — that's a Tuesday-cadence question, not verified here. Also unverified: `dashboard.deltamint.app`'s actual production traffic (open `needs owner` item, unrelated to today's ships) and whether EU/UK consent-banner decision (open `needs owner` item from `b453a9f`) has been made.
</content>
