# 2026-09-22 — end of day

- **Shipped:** nine changes on `staging` today, all scanner/earnings robustness — none new capability. The whole-market sweep actually reaches the whole market now instead of stopping alphabetically at 4,000 of 12,647 names (`daf0282`), drops leveraged/inverse funds by name (`bc97286`), and says why it found nothing — floor rejections vs. per-ticker skip reasons — instead of a bare "no setups matched" (`a30530b`, `0adf729`). Cash-secured put, covered call and wheel scans stop coming back structurally empty: their return-on-risk floor now measures against the whole strike and defaults to 0%, and presets can finally be saved for those three strategies (migration `0057`, `c8d6470`) — the old 15% default and the missing preset rows were bugs, not policy. The "no earnings date" signal moved off the results table and into the trade ticket, in words, at the point of commitment (`e0d4906`, `56984b9`). Also live: an eleventh foundations post, early exercise (`da81319`/`8568a79`/`08202d0`), diagrams still not rendering — same tracked gap as the last two posts. `docs/ops/shipped.md` on `staging` was already caught up through the 20:10 UTC duty-engineer run; nothing to add.
- **Backlog:** unchanged, three of five slots used, last ranked 2026-09-01. None of today's commits touch the open proposals — no bid/ask width column, no POP ranking, no `scan_runs` table. Worth flagging for Tuesday, not fixing today: the CSP/covered-call/wheel floor bug fixed today means those strategies were effectively unusable in the whole-market sweep until this morning, which is new context for how much weight to put on "five strategies" claims that assumed they worked.
- **Positioning:** unchanged. Today's commits make the scanner behave the way `docs/product/features.md` (S2, S3, S12) and the market-scan claims already describe — CSP/CC/wheel scans and their presets were silently broken, now they work; the whole-market claim was silently alphabetical, now it's whole. Nothing here is a new capability or a new competitive fact, so `docs/context/positioning.md` and `docs/product/features.md` are left as is. (`features.md` is dated 2026-09-02 and is stale well beyond today's scope — S2 still describes an S&P-500-only universe that shipped over on 2026-09-09 — but a full rewrite is outside a daily pass; flagging for the next Tuesday run rather than doing it piecemeal here.)
- **The page:** no ticket. `landing/public/index.html`'s scanner replica already shows the dated-announcement earnings chip the same way the real table still does, and its demo strategy list already includes cash-secured put/covered call/wheel with no claim about their floor or their presets — nothing it shows was made false or newly incomplete today. Note for the record: the brief's `landing/public/pricing/index.html` no longer exists (deleted in `95b03b7`, when billing moved behind `/billing` in-app, gated by `billing_visible`) — pricing isn't a static page anymore.
- **Not gotten to:** the blog-diagram deploy gap (three posts, six images, tracked in `docs/ops/queue.md`) is duty-engineer's/agent-manager's territory, not touched here. Didn't re-verify the competitor facts in `positioning.md` (that's `market-watch`'s job) or re-check `pricing.md` against the live `/billing` screen.
