# 2026-09-08 — end of day

- **Shipped:** eight `staging`-only commits today, all Positions Monitor accuracy work plus one new feature — a per-ticker combined P/L-at-price curve for accounts holding more than one position on a name (`fbd520e`); a stock repair now reads as one `call_ratio_spread` position instead of splitting across cards (`6770ae4`, `5bd85f7`); account risk/cover fixes so an unbounded position is flagged unknown rather than counted as zero, stress totals stop double-counting, and cover draws from long options before shares (`df1f997`); a close ticket can no longer offer more shares than a row actually holds (`c642dcc`); the dashboard now shows true broker quantity with covered-call shares named alongside it (`dcb5f5f`); a pairing-engine bug that could vanish two legs off a live account's dashboard, fixed (`92a78ed`); the hourly broker feed capture now also records raw positions and open orders for support (`dcd6b34`, migration 0028); and the app itself now deploys via a real `wrangler deploy` instead of an unrouted Cloudflare preview (`9183b42`). `docs/ops/shipped.md` on this branch was missing all eight plus five `staging` entries from 2026-09-07 (main's copy of the file had gone stale) — replaced with `origin/staging`'s copy, which duty-engineer had kept current all day.
- **Backlog:** unchanged. None of today's eight touch the three open proposals (quoted exit width, POP ranking, scan-run recording) — all eight are Positions Monitor/Trade History correctness and infra, not screener capability. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** `docs/context/positioning.md` unchanged — nothing today flips a "Not yet built" claim to built or touches a competitor fact; the ticker P/L curve deepens the existing "Real-time comprehension of many holdings — Real edge" claim rather than changing its truth value. `docs/product/features.md` got two factual rows (P6b stock repair, P18 the ticker curve) and a note that the `main`/`staging` gap (73 files) is now large enough to need a full re-walk on Tuesday.
- **The page:** no wording ticket. All eight commits are `staging`-only, so `landing/public/pricing/index.html` and `landing/public/index.html` claim nothing today's shipments changed. But `92a78ed`'s own commit message says the leg-vanishing bug it fixes was "reported on a live account" — meaning the unpatched code is still on `main` today, so the pricing page's "every position grouped as it was traded" claim can currently be false for a live account shaped like that report. Filed as an escalation in `docs/ops/queue.md` for `agent-manager` to prioritize the release review, not a page-wording problem.
- **Not gotten to:** the debit-vertical mis-filing escalation from the 18:08 duty-engineer run (Trade History groups a closed debit spread as two unpaired legs) is still open and outside this question. The Cloudflare-build `needs owner` ticket (`932106b`) is unresolved. Pricing/packaging implications of `call_ratio_spread` and the ticker curve (which tier they sit in) were not assessed — this was a shipped-vs-claims pass, not a pricing review; that's Tuesday's job once these are further from `staging`.
