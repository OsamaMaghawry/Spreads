# Tuesday product cadence — 2026-09-08

vp-product. Reconciled against `docs/ops/shipped.md`, `docs/product/daily/`
(02/03/04 Sep — no daily notes exist for 05–07 Sep), `docs/product/features.md`,
and every commit on `main` since the last Tuesday run (2026-09-01).

## 1. The backlog as it stands

Unchanged from 2026-09-01, all three proposals still open, none shipped or
half-shipped this week. Full reconciliation and evidence now recorded in
`docs/product/backlog.md`. In one line each:

1. **Show the executable exit cost on scan results (quoted width).** Still
   open — no width/bid-ask column shipped, no floor field.
2. **Probability-of-profit column and sort.** Still open — `scanCandidates`
   still ranks by `returnOnRisk` only.
3. **Record what was scanned (`scan_runs`).** Still open — no migration for
   it exists; a scan still leaves no trace on the server.

Two slots deliberately empty, as of 2026-09-01 (fewer, better-tested entries
over a full cap).

## 2. Kills

None this run. "Universe demand instrumentation" was killed 2026-09-01 (see
`backlog.md`); nothing new qualifies this week — no proposal's kill
criterion was tested against new data, because the week's ships didn't touch
scanner ranking, columns or logging.

## 3. Pricing

No change to the number. **$29/mo, $290/yr, two tiers, held** — the
2026-09-02 decision stands; no new evidence this week moves it.

Two corrections made to `docs/product/pricing.md` itself, both to make the
file true against the code, not to change the proposal:

- **§1 "no billing exists" was wrong and had been for five days.** Stripe
  checkout, the billing portal and the webhook handler all exist
  (`supabase/functions/createCheckoutSession`, `billingPortal`,
  `stripeWebhook`), gated off by `app_settings.billing_enforced` (seeded
  `false`). Nothing is charged or shown until the owner flips it — that
  practical claim was always right; the "no code exists" sentence was not.
  Caught by this week's reality check (`docs/reality/2026-W37.md`), fixed
  here.
- **§6 item 1 marked done.** The pricing page already reads "Paper is free.
  Live is $29 a month, flat" — verified against the live repo copy of
  `landing/public/pricing/index.html`, not memory.
- **The $19-vs-$29 conflict the Friday board pack raised (`docs/board/
  2026-W36.md`, decision 1) is resolved, not reopened.**
  `docs/product/positioning-independent.md` is the same-day, same-premise
  case pricing.md's §3 already named and superseded — it was written before
  the 1–2 Sep shipments that moved the product into Tiblio's category. No
  new evidence since 2 Sep argues for $19. Left in place as the record of
  an independent pass, not a live second number. Noted in `pricing.md` §3.

## 4. Positioning

**No edit to `docs/context/positioning.md` this run — checked, not
skipped.** Its Tiblio numbers ($34.95/mo, ~$35/month anchor) match the
2026-09-01 teardown (`docs/product/teardowns/tiblio-trade-desk.md`,
verified vendor pricing page via WebSearch, cross-checked by market-watch
independently the same day) exactly; nothing shipped this week adds a
broker, a strategy, or closes a competitor gap — the week's product ships
were bug fixes on already-typed positions and a homepage redesign that
makes existing claims more visible, not new claims. `features.md` (last
rewritten 2026-09-02/03) is likewise still current: no commit since
2026-09-04 touches Screener, Trading, Positions Monitor, Trade History, or
Analysis rows.

## 5. Decisions needed from the owner

None new from this run. Everything below is a re-surfaced open item, not a
new ask:

- Stripe test-mode setup (`docs/ops/queue.md`, 2026-09-02) — blocks the
  billing switch from being testable even in staging.
- Who the watch emails (pricing.md §6 item 8) — blocks "alerts" appearing
  on the pricing page.
- The research allowlist (`tiblio.com`, `optionstrat.com` apex,
  `optionalpha.com`, `quantwheel.com`) — every competitor price this file
  and `positioning.md` cite is still `reported`, not `verified`, until it
  opens.

## 6. What could not be verified, named plainly

- **Whether `dashboard.deltamint.app`/`deltamint.app` serve what `main`
  holds.** Both are 403 at the proxy CONNECT this week (`docs/context/
  reachable.md`, re-tested 2026-09-04 and again by this week's reality
  check on 2026-09-07) — not a site outage, a session-side block. Recorded,
  not worked around. The pricing-page and homepage claims above are
  verified against the repo copy, not the live response.
- **Whether the homepage rebuild (2026-09-07) or the pricing-page fix
  (2026-09-02) actually reached production traffic**, given the open
  question (`docs/ops/queue.md`, 2026-09-03) that the app/landing Cloudflare
  Workers' build step may only upload a preview version. Outside this
  question — flagged, not investigated; belongs to whoever owns the deploy
  pipeline.
- **Silence elsewhere reads as clearance:** nothing else in `features.md`,
  `pricing.md`, or `positioning.md` needed a change this run beyond the two
  corrections in §3 above.
