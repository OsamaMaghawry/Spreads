# 2026-09-15 — end of day

- **Shipped:** the big event was `c835e0f` (dated 09-14, landed inside the last 26h): it fixed the close-ticket dead-zone crash that had made closing any position throw a blank white screen since that morning's order-card release, added the app's first error boundaries, and — because it's a merge of the full `staging` branch, not a cherry-pick — carried everything still tagged `(staging)` in `shipped.md` onto production `main` in one move (saved-order-ticket restore, the eighth foundations post going fully live, Broker tab, whole-market scan, wheel single-leg writes, demo mode, the `dumpBrokerFeed` auth hole, and more). Verified: `git diff --stat origin/main origin/staging` now shows zero `src/`/`supabase/` difference. `shipped.md` was missing the close-ticket line and had two stale `(staging)` tags on entries that shipped in the same commit — added/fixed. The rest of the file's older `(staging)` lines were left as dated, with one new entry pointing at this merge rather than rewritten one by one.
- **Backlog:** unchanged, no edits. Checked all three open proposals (quoted exit-width column, POP ranking, `scan_runs` recording) against the code that just went live — none shipped: `ResultsTable.jsx`'s "width" column is still spread-wing width, not bid/ask; `scanEntries`/`findEntry` still insert nothing. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** `docs/context/positioning.md` unchanged — nothing today changes a competitor fact. `docs/product/features.md` updated: rows S3 and T1 (wheel single-leg scan + trade) moved from `staging` to `main` with the merge commit cited, the `dumpBrokerFeed` back-office line's stale "migration 0024 on staging" corrected, and the packaging note that called wheel single-leg writing "the next product build" rewritten — it shipped today, not next.
- **The page:** no ticket. `landing/public/pricing/index.html` doesn't exist (billing is still hidden pending `billing_visible`; the live pricing surface is a draft at `landing/drafts/pricing/index.html`). Checked the draft and `landing/public/index.html` for wheel/single-leg/covered-call claims — both already describe the wheel and single-leg strategies as available, which today's shipment makes true rather than contradicts.
- **Not gotten to:** did not hand-verify every older `(staging)`-tagged `shipped.md` line individually against the merge (dozens of entries, Sep 2–14) — flagged as reached-`main` via the one new note rather than rewritten line by line; worth a closer pass if any of those specific claims get quoted externally before then. Also did not re-check pricing draft's "no price and no date yet" automation line against anything, since automation itself didn't ship today.
