# 2026-09-24 — end of day

- **Shipped:** fourteen changes reached `main` today, all Alpaca compliance and
  marketing-site work, none of it scanner/trading code. Connect now shows
  DeltaMint's own authorization disclosure before the Alpaca redirect
  (`c6f07b8`); PDF copies of Terms, Privacy and a (never-linked) pricing
  schedule now exist for Alpaca's review, generated from the live pages rather
  than hand-maintained copies (`38679e2`, `c6e8df5`, `df68c09`); the Alpaca
  logo is back in the homepage integrations section after a false start
  replaced it with text (`7433d2e`); the share/social preview card now shows
  the actual product instead of a word mark, and staging previews staging's
  own card (`fa9d66b`, `2435efe`, `abee00f`); the site menu gained Home/About
  (with a new About page) and a Sign up button, and the broker card now reads
  "Supported brokers" instead of "Live now" (`d9b71e6`, `821fd62`); the
  homepage's phone hero shows two DeltaMint cards instead of four, with a "+2
  more" line so the account total still reconciles (`c5ea547`); the free
  trial dropped from 30 days to 7 on both intervals, and the Terms now admit
  yearly billing exists (`8c918dc`); a branding-review PDF now covers the
  About page (`7a3d2ba`); and a thirteenth foundations post — buying power,
  margin and collateral — published after seo-editor/desk-editor review
  (`26a7513`, `490b7fd`, `dacb16e`, `cda8571`). `docs/ops/shipped.md` already
  has every line — duty-engineer wrote it on `staging` before this run, ahead
  of the next merge to `main`; nothing to add.
- **Backlog:** unchanged, still three of five slots (last ranked 2026-09-01).
  Checked directly: none of today's commits touch `optionScan.ts`,
  `scanEntries`, `findEntry`, `Screener.jsx`, `scanPresets.js` or `sp500.js` —
  the files the three open proposals (quoted exit width, POP ranking, scan
  recording) depend on. Today's work is compliance and site chrome, not
  scanner capability; no re-scope, no kill.
- **Positioning:** unchanged. Nothing today changes what DeltaMint does or how
  it compares to Tiblio/Barchart/the rest — the Alpaca disclosure, the PDFs,
  the About page and the share card are compliance and presentation, not
  product capability, so `docs/context/positioning.md` and
  `docs/product/features.md` stand as last written.
- **The page:** no ticket needed. `landing/public/pricing/index.html` does not
  exist — pricing was deliberately pulled off the site (`c6e8df5`: "not a page
  as it's not live yet"), replaced by an unlinked PDF for Alpaca's compliance
  review only, so there is no live pricing claim to check. `landing/public/index.html`
  and the new `/about` page were checked against today's trial-length and
  billing-terms change (7 days, monthly-or-yearly); neither page states a
  trial length or billing cadence, so nothing on them was made false by
  `8c918dc`.
- **Not gotten to:** this pass didn't re-verify anything against the live
  site — `deltamint.app` is still 403 at CONNECT from this session (standing
  item, `docs/context/reachable.md`), so today's homepage/About/share-card
  read is off the repo, not a browser. The queue's long-standing `needs
  owner` items (Brevo sender verification, Tradier/SnapTrade secrets, the
  `www.deltamint.app` 522, migration confirmations) are outside this
  question and untouched.
