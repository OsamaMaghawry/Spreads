# 2026-09-07 — end of day

- **Shipped:** four things reached `main` in the last 26 hours. The homepage rebuild (hero replay, screener/ticket replicas, individual-legs section) and its two follow-up polish commits were already ledgered; three were missing and are now added — the email digest actually sends (eleven agent reports stranded since 2 Sep were landed and mailed, plus a stray-asterisk bug and doubled subject fixed, `a7cea26`/`ad3eb33`), the digest now leads with the owner's open "Needs owner" queue items instead of only agent activity and collapses a large batch into an index (`1ac077a`), and the public blog went from two posts to four, restructured to lead with points and reset to reading-size type (`fcfefbd`). `docs/ops/shipped.md` updated.
- **Backlog:** unchanged. None of today's shipments touch the three open proposals (quoted exit width, POP ranking, scan recording) — today's work is content and internal-ops tooling, not scanner capability. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** `docs/context/positioning.md` unchanged — nothing today bears on the market or competitor picture. `docs/product/features.md` did need a fix, found while checking today's landing changes against the code: rows S3 and T1 (cash-secured put / covered call / wheel scanning and the single-leg open ticket) were still marked `staging`, but `git log` on `main` shows both shipped there on 2 Sep (`f8c427b`, `acc57a4`) — five days stale. Corrected in place.
- **The page:** no ticket. The rebuilt homepage's screener panel claims cash-secured put, covered call and wheel scanning (`landing/public/index.html` line 437) — that looked like it might be ahead of the code given `features.md` said "staging," but the code check above shows it is in fact on `main`; the page was right and our own inventory was wrong. `pricing/index.html` was re-read against current `main` and still matches what ships.
- **Not gotten to:** the scan-recording proposal (#3) that would let a POP or width column be evaluated with real usage data is still unbuilt. `docs/ops/queue.md`'s 2026-09-07 entry — `228fea9` (market-hours message, scan-loop backoff) merged to `staging` on 4 Sep with no PR to `main` — is duty-engineer's flag to the owner, not touched here.
