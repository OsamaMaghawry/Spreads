# 2026-09-04 — end of day

- **Shipped:** six changes reached `main` today — held shares are no longer offered for sale twice (`d1c3262`), the orders row and close ticket stop calling a share order a "leg" (`fc9598f`, `08b1fe5`), a share position finally closes from the ticket without trapping the order (`326ce67`), the position watch stops mailing duplicate lines and mailing nothing-to-report runs (`f0710c8`), and Hotjar session replay went live on the public site under the same hostname gate as GA (`cc855e7`, `b453a9f`); one more (`228fea9`, scanner tells "market shut" from "feeds disagree") is verified on staging and not yet on `main`. `docs/ops/shipped.md` was missing all six lines — added.
- **Backlog:** unchanged. None of today's six touch the three open proposals (quoted exit width, POP ranking, scan recording) — all six are bug fixes and robustness work on already-shipped surfaces, not new scanner capability. Still three of five slots used, last ranked 2026-09-01.
- **Positioning:** unchanged. Today's fixes make existing claims *true* (a share position can now actually be closed, which `pricing/index.html`'s "close any position, any leg, any time" already asserted) rather than adding anything new to claim; nothing here changes the competitor comparison in `docs/context/positioning.md` or the inventory in `docs/product/features.md`.
- **The page:** no ticket needed. `landing/public/pricing/index.html` already says "Close any position, any leg, any time" and lists "shares from assignment" — today's `326ce67` fix makes that line honest rather than aspirational; no material claim is now false or missing.
- **Not gotten to:** `228fea9` (market-shut vs. feeds-disagree) is staging-only — worth confirming it merges to `main` before it's counted as shipped in tomorrow's pass. Two open `needs owner` items from the queue (EU/UK consent banner for GA/Hotjar; the Cloudflare Workers production-promotion question) sit outside this question and were not touched.
