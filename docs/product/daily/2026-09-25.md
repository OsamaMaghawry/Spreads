# 2026-09-25 — end of day

- **Shipped:** one change reached `main` in the last 26 hours — a fourteenth
  foundations blog post, "how to read an option chain," closing the
  foundations series (which price on a row is live vs. the last-trade price,
  what the in-the-money shading actually follows, why a later expiration
  costs more), reviewed by seo-editor and desk-editor (`fd32cd0`, `8b8267b`,
  `55791f3`, `c1bf803`). Everything else in the window is a duty-engineer
  hourly ledger entry (no code) and the daily metrics snapshot. `shipped.md`
  was missing the post's line — added.
- **Backlog:** unchanged. Today's shipment is content, not scanner code —
  it touches none of the three open proposals (quoted exit width, POP
  ranking, scan recording), all of which live in `optionScan.ts` /
  `ResultsTable.jsx` / the screener edge functions. Still three of five
  slots used, last ranked 2026-09-01 (Tuesday's job to re-rank).
- **Positioning:** unchanged. `docs/context/positioning.md` and
  `docs/product/features.md` are about product capability and the
  competitor comparison; a foundations blog post closing out an
  educational series claims nothing about what the app does and changes no
  row in either file.
- **The page:** no ticket needed. Checked `landing/public/index.html` (no
  `pricing/index.html` exists in this repo — it's a draft at
  `landing/drafts/pricing/index.html`) and the draft pricing page; both
  link to `/blog` generically and make no claim about series length or
  content that today's post would contradict or that omits it.
- **Not gotten to:** the standing queue items (Brevo sender verification,
  Tradier sandbox token, SnapTrade's 410 holdings routes, the `www.`
  redirect, migration confirmations) are all `needs owner` and untouched by
  this pass — none of them are shipped-vs-backlog-vs-positioning questions.
  staging vs. main also carries a much larger, unrelated delta (older blog
  posts, metrics snapshots and SVGs present on one side and not the other)
  that looked at a glance like today's shipment but predates this window
  and wasn't investigated further, since it's not what shipped to `main`
  today.
