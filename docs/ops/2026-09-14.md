# 2026-09-14

## 13:16 UTC — duty-engineer run

**Read.** No run has landed since `2f4b6e0` (2026-09-11 20:06 UTC) — a gap of
just under 3 days, weekday hours included. `git log --oneline 2f4b6e0..origin/main`
and `...origin/staging` together carried 207 unique commits. `shipped.md` had
not been touched since `c7d6a65` (content stopping at 2026-09-11), so this run's
main work was the catch-up: every commit in the gap touching `src/`,
`supabase/functions/`, `landing/` or `content/blog/` was diffed against the
hashes already cited in `shipped.md`, leaving 96 substantive, uncited commits,
grouped into **16 new plain-English entries** now at the top of `shipped.md`,
covering 2026-09-07 through 2026-09-14. Largest features in the gap: the
Orders tab rebuilt (save-for-later on any order, a live Saved tab, fractional
quantities correct to Alpaca's real nine decimal places), a full option-chain
trading page (every strike/expiry, cross-expiry multi-leg, correct payoff
math), a rebuilt weekly digest email that now actually reaches every
connected account's own users, a corrected daily equity series behind it, a
new marketing homepage built from real product screens, Demo mode, a
left-side nav redesign, three security headers on both sites, the
Screener→"Strategy Scanner" rename, and five new foundations blog posts (a
sixth — implied volatility — is drafted and live on staging only, not yet on
`main`). Full list and commit hashes in `docs/ops/shipped.md`.

`docs/ops/queue.md` was last touched at `df5611c`, inside this same gap — its
current state (owner already flipped Always-Use-HTTPS and closed that
ticket; `www.deltamint.app`'s redirect-rule fix filed `needs owner`, low
priority, told once already; the Vault `service_role_key`/anon-key mix-up,
the missing `SUPABASE_SERVICE_ROLE_KEY` Actions secret, the Cloudflare
Workers Build duplicate-publisher, Stripe/GA4/ops-token setup, and the
leg-first-rebuild backlog owned by systems-engineer/head-of-trading — is all
already correctly filed as `needs owner`, `escalated` or `open` against
another agent, none of it new or actionable by duty-engineer this run. No
line needed to move.

`DELTAMINT_OPS_TOKEN` is not set in this environment — opsHealth not called;
read what's available from code, the queue and GitHub Actions history
instead, per the brief.

`https://deltamint.app`, `/blog`, `/pricing` and `https://dashboard.deltamint.app`
re-tested: all four still `CONNECT tunnel failed, response 403`, the same
recorded failure mode in `docs/context/reachable.md` (no new row needed — not
reporting "down" for hosts this session cannot reach).

Checked the last ~15 completed GitHub Actions runs across both branches for
anything red beyond what's in the queue: two `content-merge` runs failed
mid-draft on the implied-volatility post (`32186c2`, `10c343`) while
desk-editor/seo-editor findings were still being applied — both superseded by
the next push in the same chain, which went green (`1fe3c4d`, `218c8d0`).
Self-resolved by the content pipeline; nothing outstanding.

**Checks on staging.** Fresh container (`node_modules` not installed); ran
`npm ci` first, then:
- `npm run lint` — clean.
- `npm test` — 906 tests (363 + 543 across the two suites), all green.
- `npm run build` — clean (same pre-existing >500kB chunk-size warning, not new).
- `npm run context:check` — clean, `docs/product-context.md` is current.

**Fixed this run:** `docs/ops/shipped.md` — 16-entry catch-up, no code change,
committed to `staging`.

**Escalated:** nothing new — queue fully accounted for, nothing left to drain.

**Email:** not sent — no new escalation and no red check on staging. Clean
hour, ledger-catch-up aside.

## 14:08 UTC — duty-engineer run

**Read.** One new commit on `staging` since the last run: `3ab139a`
("A reopened saved ticket said 'No ceiling' because it knew nothing"),
merged in `4cdde6d`. Touches `src/` and adds migration `0053_saved_orders_setup.sql`
— a real fix already made and merged by another session, not this run's own
change — so this run's job on it was step 1 only: cite it. Added one
plain-English line to `docs/ops/shipped.md`. `docs/ops/queue.md` re-read in
full: unchanged since the 13:16 run and still fully triaged — every `open`
line belongs to systems-engineer/head-of-trading's leg-first-rebuild bench
review (explicitly pending their re-audit, not a plain bug), every
`needs owner`/`escalated` line is already correctly filed. Nothing to drain.

`DELTAMINT_OPS_TOKEN` not set — opsHealth not called, per brief. Re-tested
`deltamint.app`, `/blog`, `/pricing`, `dashboard.deltamint.app`: all four
still `CONNECT tunnel failed, response 403`, same recorded failure in
`docs/context/reachable.md` — no new row needed.

**Checks on staging.** `npm ci`, then:
- `npm run lint` — clean.
- `npm test` — 911 tests (368 + 543), all green (up from 906 — `3ab139a`
  added tests to `setupUnit.test.js`).
- `npm run build` — clean, same pre-existing >500kB chunk-size warning.
- `npm run context:check` — **red.** `docs/product-context.md` was stale:
  `3ab139a`'s migration (new `saved_orders.setup` column) and four days of
  `shipped.md` entries since the doc was last regenerated had not been
  folded in. Mechanical, auto-derived file with a documented regeneration
  command (`npm run context`) — no judgment call, so fixed directly: ran
  `npm run context`, diff is exactly the new column and the newer
  `shipped.md` lines, re-ran `context:check` green.

**Fixed this run (`021cd0f`):** `docs/product-context.md` regenerated
(red `context:check` → green); `docs/ops/shipped.md` — one new entry for
`3ab139a`. Both on `staging`.

**Escalated:** nothing new.

**PR:** none opened. `staging` and `main` are 200+ commits apart already
(a pre-existing, owner-managed release gap — `docs/ops/ship.md` is the
production-shipping process, and it is the owner's step, not duty-engineer's);
a `staging→main` PR right now would sweep in that whole backlog rather than
describe this run's fix. This run's change is bookkeeping — regenerating an
auto-derived doc and citing an already-merged commit, no behavior change —
same precedent as the 13:16 run's shipped.md-only catch-up, which also
opened no PR.

**Email:** not sent — no new escalation, and the one red check was fixed
this run, not left red.
