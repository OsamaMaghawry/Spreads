# 2026-09-17 — duty-engineer ledger

## 13:05 UTC — duty-engineer run

**What changed.** `git fetch origin && git checkout staging` — clean,
tracking `origin/staging`, tip `34ef44a` (unchanged since the last ledger
entry, 2026-09-16 20:06 UTC, HEAD `deffc31`). `git log --since="26 hours
ago" --oneline main staging`, filtered to `src/`, `supabase/functions/`
and `landing/`: nine new commits since the last run — a full arc, done by
another session between 06:45 and 08:43 UTC today, responding directly to
two owner questions about the weekly digest and Account Analysis
disagreeing with each other:

- `a1127e8` / `ec6b0f3` — `equityHistory.fetchOpenDates` filtered closed
  orders by `symbols=<contract>`, which misses a leg nested inside a
  spread's parent order; one such leg (a TSLA put bought inside a spread on
  14 Sep) had no discoverable open date and the nightly rebuild carried it
  as held-but-unpriced on every day, blanking 52 days of one account's
  stored history to `performance: null`. Fixed by paging every closed
  order and matching legs locally (new `_shared/legOpenDates.ts`, 8
  tests). Migration `0054` adds `weekly_digest_sends.figures` so what a
  digest email said is kept permanently instead of only existing in the
  recipient's inbox.
- `f6d610c` — Account Analysis and the weekly digest measured a window's
  baseline from two different days; the digest silently excluded
  provisional trades the page counted; the gap between "what trading did"
  and the broker's own account-value change had no explanation on screen.
  All three reconciled.
- `33b1333` — the Analysis chart now draws the close a window is measured
  FROM (previously the first drawn point was already inside the window,
  so a week's whole move sat off the left edge); a new `WindowParts` panel
  breaks a window's total into the same four parts the digest emails.
- `7da283d` / `22c5b44` — stored history is now frozen: a rebuild may not
  blank or materially change a day older than the last five sessions
  (this is the guard that would have caught the 16 September incident);
  every sent digest is now re-checked against what's actually stored, and
  the owner is mailed once per new drift finding, not every night.
- `34ef44a` — deploy-workflow fix only (authenticates the Supabase CLI
  setup action so a GitHub API rate limit can't fail a deploy); no
  `src/`/`supabase/functions/`/`landing/` change, no `shipped.md` line.

All of this is **already on `main`** — verified by diff, not by trusting
the branch: `git diff origin/staging origin/main -- src supabase/functions
landing` is empty; main carries the same changes under different commit
hashes (`7386f6b`, `bf9180f`, `0dd5122`, `e8787b0`, `294175c`, `a9dbf8b`,
`2a2287c`), landed directly rather than through `staging`'s own PR flow.
`shipped.md` had no line for any of it — added four (oldest four entries
above), newest first, per the ledger's own rule.

**What broke.** `docs/ops/queue.md` read in full, oldest first. One new
finding this run, made while reading the arc above: migration `0054` adds
`weekly_digest_sends.figures`, and `weeklyDigest/index.ts:351` upserts a
row naming that column unconditionally — the identical shape to the
`0053`/`saved_orders.setup` case confirmed and closed on 2026-09-14. If
`0054` is not applied on production before the `weekly-digest` cron next
fires (Saturday 13:00 UTC, two days out), the upsert fails with
`PGRST204` for every account and no digest reaches anyone that week, every
week, until it's applied. Migration, unreadable from here, on production:
escalate, not fix. Filed as `needs owner` in `docs/ops/queue.md` with the
deadline and the exact line number; emailed (see below).

The queue's own top item (`[fixed 2956bfb]`, the leg-blanking finding
above) still carries one open line — "one unplaceable leg nulling the
entire series is honest and catastrophic; the bound belongs to
head-of-trading, not a patch" — already correctly filed as a design
question in the same run it was found; nothing new to add this run.
Every other line is unchanged: `needs owner` (Cloudflare/DNS/Stripe/
secrets/Vault, owner-only) or `open`/`escalated` bench findings on the
leg-first rebuild assigned to systems-engineer/head-of-trading/
investment-analyst/tax-accountant — outside duty-engineer's hand-off.

PR [#9](https://github.com/OsamaMaghawry/Spreads/pull/9) re-checked: still
open, unmerged, `mergeable_state: clean`, head `34ef44a`, still only the 4
Cloudflare deploy-status bot comments — no owner response since
2026-09-14. Its body is now stale (predates today's arc and migration
`0054`) but it is the owner's own merge decision to make, not
duty-engineer's to edit or close.

`DELTAMINT_OPS_TOKEN` not set in this environment (checked `env`) —
opsHealth not called, per brief.

**What is up.** Fetched `deltamint.app`, `/blog`, `/pricing` and
`dashboard.deltamint.app` directly: all four `CONNECT tunnel failed,
response 403` at the egress proxy — same failure mode already recorded in
`docs/context/reachable.md`; no new row needed.

**Checks on staging.** Fresh container (`node_modules` absent); ran
`npm ci`, then:
- `npm run lint` — clean.
- `npm test` — 954 tests (386 + 568, up from 917/374+543 — the arc above
  added tests), all green.
- `npm run build` — clean, same pre-existing >500kB chunk-size warning.
- `npm run context:check` — **red**, entirely because of this run's own
  `shipped.md` addition (the generator mirrors `shipped.md`'s newest
  lines into `docs/product-context.md`). Fixed by running `npm run
  context` and committing the regenerated file; re-ran `context:check`
  clean afterward. Not a pre-existing failure from another session.

**Fixed this run:** nothing beyond the ledger's own bookkeeping
(`shipped.md` entries, regenerated `product-context.md`) — no application
bug found that was inside duty-engineer's authority to fix.

**Escalated:** migration `0054` production-confirmation, filed in
`docs/ops/queue.md` as above, with a Saturday 13:00 UTC deadline.

**PR:** none opened — no application code changed this run; the four
`shipped.md`/`product-context.md`/`queue.md` lines are docs-only and ride
along with this ledger's own commit to `staging`.

**Email:** money/user-path finding with a hard deadline — attempted via
`sendDigest` per the brief's recipe; `yecfbeohyakuoyczvdbj.supabase.co` is
still 403 at CONNECT from this session, the same standing block recorded
since 2026-09-09 (`docs/context/reachable.md`). `PushNotification` is
present in this session (unlike some earlier runs) and was used instead,
same workaround as the 2026-09-09 run: sent a one-line push naming the
finding and the deadline. No PR opened this run to carry it further, since
no application code changed — the queue/ledger push itself is the
record.
