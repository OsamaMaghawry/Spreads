# 2026-09-21 — duty-engineer ledger

## 13:08 UTC — duty-engineer run

**Read.** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md` in full. `git fetch origin && git checkout
staging` succeeded cleanly — this run worked on `staging` itself.

**What changed.** Last ledger entry was 2026-09-18 20:05 UTC, HEAD `24c9c99`.
No weekday runs fired 19–20 Sep (Sat/Sun); this is the first run since.
`git log --since="26 hours ago" --oneline main staging` undercounts the gap,
so the comparison was done directly against the last ledgered HEAD instead.

On `staging`, 7 new commits since `24c9c99`:
- `c0332de`, `dd706f2` (19 Sep) — Analysis: setups are now grouped and listed
  correctly (owner-reported bugs, already fixed by the sessions that made
  them). Touch `src/` — added to `shipped.md`, which did not yet carry them.
- `3a7e7ed` (20 Sep) — internal vs customer email sender split. Touches
  `supabase/functions/_shared/email.ts` and `equityHistory/index.ts` —
  added to `shipped.md`. Its own commit message flags an unverified
  assumption (`agents@deltamint.app` verified in Brevo) — filed to the queue
  as `needs owner` below; not money-path or user-visible, so not emailed.
- `e34b3e9` (20 Sep) — content-merge gate diff-base fix. `.github/workflows/`
  only, no `src/`/`supabase/functions/`/`landing/` change — no shipped.md
  line per the ledger's own scope.
- `cb49619`, `e60cf06`, `c1fddba` (20 Sep) — `scripts/site-health.mjs` only,
  same reasoning — ops tooling, not a shipped product change.

On `main`, since `e362a98` (18 Sep): three metrics snapshots and the
foundations 9 (gamma) and 10 (option assignment) blog posts, each through
the full content-engine → seo-editor → desk-editor → compliance-gate chain
per `content/PLAN.md`. `content/` only — outside `shipped.md`'s scope, and
the daily content cadence's own ledger, not duty-engineer's.

Regenerated `docs/product-context.md` after the `shipped.md` edit
(`npm run context`) — same mechanical fix prior runs have made.

**What broke.** `docs/ops/queue.md` read in full, oldest first. Every dated
line already carries a state (`needs owner`, `fixed`, `escalated`) from the
runs that filed it; the standing systems-engineer/head-of-trading/
investment-analyst/tax-accountant leg-first-rebuild findings (2026-09-08/09)
remain explicitly pending their own bench re-audit — design territory, not a
duty-engineer fix. One new item filed this run (Brevo sender verification,
above). `DELTAMINT_OPS_TOKEN` not set in this environment (checked directly)
— opsHealth not called on either project, per brief.

**What is up.** Fetched `https://deltamint.app`, `/blog`, `/pricing` and
`https://dashboard.deltamint.app` directly: all four `CONNECT tunnel
failed, response 403` at the egress proxy — the same standing block already
recorded in `docs/context/reachable.md`. No new row needed; not reported as
"down".

**Checks on staging.** Fresh container (`node_modules` absent); `npm ci`
clean, then:
- `npm run lint` — clean.
- `npm test` — 1,032 tests (407 client + 625 server, up from 1,004 on
  18 Sep — the Analysis and email-sender commits added tests), all green.
- `npm run build` — clean, same pre-existing >500kB chunk-size warning, not
  new.
- `npm run context:check` — **red** on first pass, from this run's own
  `shipped.md` addition. Fixed by `npm run context`; clean on re-run.

**Fixed this run:** no application bug — none found inside plain-bug
authority; the checks were green on the merged history before this run's
own bookkeeping edit touched `context:check`. Changes are `shipped.md` /
`docs/product-context.md` / `docs/ops/queue.md` / this ledger, on `staging`.

**Escalated:** one new item — `agents@deltamint.app` Brevo sender
verification (queue, `needs owner`, 2026-09-21). Not money-path or
user-visible, so no email.

**PR:** none opened — no application code changed this run.

**Email:** not sent — nothing money-path or user-visible needs the owner
this run. Clean hour, ledger + one queue line.

## 14:06 UTC — duty-engineer run

**Read.** `.claude/agents/duty-engineer.md`, `docs/context/org.md`,
`docs/ops/README.md`, `docs/ops/queue.md` in full; the 13:08 section above, so
this run does not repeat it. Worked on `staging` at `c088db5`, clean and up to
date with `origin/staging`.

**What changed.** `git log --since="26 hours ago" --oneline main staging`:
six commits, all already accounted for by the 13:08 run. The only one inside
the window that touches `src/`, `supabase/functions/` or `landing/` is
`3a7e7ed` (the support/agents sender split), which is already in
`shipped.md`. `c088db5` is that run's own ledger commit, docs only. On `main`,
`59a5852` (foundations 10 — option assignment) and `3e5c4cd` (metrics
snapshot) are the content and metrics cadences' own ledgers, outside
`shipped.md`'s scope, as the 13:08 run recorded. **No new `shipped.md` lines
this run.**

**What broke.** `DELTAMINT_OPS_TOKEN` is **not set** in this environment
(checked directly), so `opsHealth` was not called on either project — no order
errors, walks, alerts, broker-connection issues, watch runs or daily-report
state could be read this hour. This run is code-and-CI only, which is the
standing consequence of the 2026-09-02 `OPS_TOKEN` queue item. Read the code
and the CI history instead, and that is where both of this run's findings came
from.

**What is up.** `https://deltamint.app`, `/blog`, `/pricing` and
`https://dashboard.deltamint.app` all fetched directly: four
`CONNECT tunnel failed, response 403` at the egress proxy. Already recorded in
`docs/context/reachable.md` (row dated 2026-09-04) — no new row, and **not**
reported as down. The dashboard's `X-Robots-Tag` could not be read for the
same reason; `site-health.yml` run #57 on `main` (12:43 UTC today) is green and
now asserts that header itself, which is what `c1fddba` added yesterday.

**Checks on staging.** Fresh container, `npm ci` clean, then all four green at
`c088db5`:
- `npm run lint` — clean.
- `npm test` — 407 client + 625 server, 1,032 pass, 0 fail.
- `npm run build` — clean; same pre-existing >500 kB chunk warning, not new.
- `npm run context:check` — `docs/product-context.md is current.`

**Fixed this run:** nothing. Neither finding below is inside plain-bug
authority, and no failing application code was found.

**Escalated — two, both from the CI history rather than the code.** The repo
has exactly two red runs standing, and neither had been read:

1. **Every blog diagram published since 14 September is a broken image on
   `deltamint.app`.** *Deploy landing site* last succeeded on 2026-09-14
   (run #13, `c835e0f`). Since then four SVGs reached `main` and none reached
   the Worker's asset bundle, and two live posts reference them:
   `/blog/gamma-options-meaning` and `/blog/option-assignment-what-happens`.
   Two independent causes — `content-merge.yml` dispatches `publish-blog.yml`
   but not `deploy-landing.yml`, so the deploy is never started (a
   `GITHUB_TOKEN` push starts no workflows); and when it *is* started it
   refuses, because the staging-first gate compares `HEAD:landing` against
   `origin/staging:landing` and the content gate merges posts to `main` only.
   Full evidence, the four filenames and a proposed patch are in
   `docs/ops/queue.md`. Not fixed here: both halves change how the
   **production** marketing site deploys, and the gate's destination was
   deliberately redesigned yesterday in `e34b3e9`/`9f9d4c8` — design decision,
   escalate-never-fix. Not verified against the live site, because
   `deltamint.app` is unreachable from here; stated as an inference from the
   deploy history and `landing/wrangler.jsonc`, not as a fact off a browser.

2. **The staging edge functions have not deployed since 2026-09-20.**
   *Deploy edge functions (staging)* run #148 failed on `3a7e7ed` at
   `npm run context:check` (stale generated doc, since regenerated), and the
   workflow is path-filtered to `supabase/functions/**`, so a docs-only commit
   does not retry it. The sender split is right in the repo and not live on the
   staging project. Staging only; production unaffected. Queue entry has the
   detail.

**Email: attempted, refused, and that is now twice on the record.** Finding 1
is user-visible, so the brief requires the email. Built the message and posted
it to `sendDigest` on production with `.env.production`'s anon key per the
recipe: `curl: (56) CONNECT tunnel failed, response 403`. Re-tested the staging
project's function host in the same run: identically 403. So the 2026-09-14
`needs owner` item — "the duty engineer can no longer reach the owner at all" —
still holds, unchanged, and no agent-side workaround exists that does not
defeat the allowlist. `email-digest.yml`'s push paths still do not cover
`docs/ops/**`, and filing an ops escalation under `docs/product/**` to trip a
path filter remains the wrong trade. Used the one channel that works: a pull
request: **[PR #11](https://github.com/OsamaMaghawry/Spreads/pull/11)**,
`duty/2026-09-21-blog-diagrams` → `main`, docs only, **not merged** —
`docs/ops/2026-09-21-needs-you-blog-diagrams.md` carries the ask in the words
the email would have used. (A second `staging` → `main` PR could not be opened:
#9 is still open for that pair. The GitHub MCP tools named in this run's task
were not surfaced to the session either — the PR was opened through the REST
API with the session's own token.)

**PR:** one, [#11](https://github.com/OsamaMaghawry/Spreads/pull/11), the
escalation above. No application code changed this run, so there is no fix PR.

## 15:06 UTC — duty-engineer run

**Read.** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md` in full; both sections above, so this run
does not repeat them. `git fetch origin && git checkout staging` succeeded
cleanly, at `ebeccb5` (this run's own working HEAD, unchanged from 14:06).

**What changed.** `git log --oneline ebeccb5..staging` and
`3e5c4cd..main` are both empty — no commits have landed on either branch
since the 14:06 run closed. Nothing new for `shipped.md`.

**What broke.** `docs/ops/queue.md` read in full again, oldest first. Every
line already carries a state from a prior run or the owner, and none of the
standing `needs owner`/bench items are duty-engineer's to act on (design
decisions, credentials, or waiting on the owner). No new item to file.
`DELTAMINT_OPS_TOKEN` still not set (checked directly) — `opsHealth` not
called on either project, same standing consequence as the prior two runs
today.

**What is up.** `https://deltamint.app`, `/blog`, `/pricing` and
`https://dashboard.deltamint.app` all re-fetched directly: four `CONNECT
tunnel failed, response 403` at the egress proxy, identical to the 13:08 and
14:06 runs and to the standing row in `docs/context/reachable.md`. No new
row; not reported as down.

**Checks on staging**, fresh container, `npm ci` clean, all green at
`ebeccb5`:
- `npm run lint` — clean.
- `npm test` — 407 client + 625 server, 1,032 pass, 0 fail.
- `npm run build` — clean; same pre-existing >500 kB chunk-size warning, not
  new.
- `npm run context:check` — `docs/product-context.md is current.`

**Queue drain.** Both duty-engineer escalations already on the board from
the 14:06 run are unchanged: PR [#11](https://github.com/OsamaMaghawry/Spreads/pull/11)
(blog diagrams, `duty/2026-09-21-blog-diagrams` → `main`) and PR
[#9](https://github.com/OsamaMaghawry/Spreads/pull/9) (`staging` → `main`,
migration confirmation) are both still open, not merged — checked via the
GitHub MCP tools, which are available in this session. No new ticket to
open or close.

**Fixed this run:** nothing — no failing code found, checks were already
green going in.

**Escalated:** nothing new.

**PR:** none opened — no application code changed and nothing new to
escalate.

**Email:** not sent — nothing new is money-path or user-visible this run;
the two open escalations already have their PRs on record from 14:06. Clean
hour, ledger line only.

## 16:06 UTC — duty-engineer run

**Read.** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md` in full; all three sections above, so this
run does not repeat them. `git fetch origin && git checkout staging`
succeeded cleanly, at `f613c9e` (the 15:06 run's own ledger commit).

**What changed.** `git log --oneline f613c9e..origin/staging` and
`3e5c4cd..origin/main` are both empty — no commits have landed on either
branch since the 15:06 run closed. Nothing new for `shipped.md`.

**What broke.** `docs/ops/queue.md` read in full again, oldest first (both
halves, including the "Open" section's standing systems-engineer/
head-of-trading leg-first-rebuild findings from 2026-09-08/09). Every line
still carries a state from a prior run or the owner; none are duty-engineer's
to act on (design decisions, credentials, or waiting on the owner/bench). No
new item to file. `DELTAMINT_OPS_TOKEN` still not set (checked directly) —
`opsHealth` not called on either project, same standing consequence as the
three runs today before this one.

**What is up.** `https://deltamint.app`, `/blog`, `/pricing` and
`https://dashboard.deltamint.app` all re-fetched directly: four `CONNECT
tunnel failed, response 403` at the egress proxy, identical to every run
today and to the standing row in `docs/context/reachable.md`. No new row;
not reported as down.

**Checks on staging**, fresh container (`node_modules` absent from this
session; `npm ci` clean), all green at `f613c9e`:
- `npm run lint` — clean.
- `npm test` — 407 client + 625 server, 1,032 pass, 0 fail.
- `npm run build` — clean; same pre-existing >500 kB chunk-size warning, not
  new.
- `npm run context:check` — `docs/product-context.md is current.`

**Queue drain.** Checked both open duty-engineer PRs via the GitHub MCP
tools: [#11](https://github.com/OsamaMaghawry/Spreads/pull/11) (blog
diagrams, `duty/2026-09-21-blog-diagrams` → `main`) and
[#9](https://github.com/OsamaMaghawry/Spreads/pull/9) (`staging` → `main`,
migration confirmation) are both still open, correctly not merged, unchanged
since 15:06. No new ticket to open or close.

**Fixed this run:** nothing — no failing code found, checks were already
green going in.

**Escalated:** nothing new.

**PR:** none opened — no application code changed and nothing new to
escalate.

**Email:** not sent — nothing new is money-path or user-visible this run.
Clean hour, ledger line only.

## 17:06 UTC — duty-engineer run

**Read.** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md` in full. `git fetch origin && git checkout
staging` succeeded cleanly, at `753e3d3` (the 16:06 run's own ledger commit).

**What changed.** `git log --oneline 753e3d3..origin/staging` and
`3e5c4cd..origin/main` are both empty — no commits have landed on either
branch since the 16:06 run closed. Nothing new for `shipped.md`.

**What broke.** `docs/ops/queue.md` read in full again, oldest first. Every
line still carries a state from a prior run or the owner; none are
duty-engineer's to act on (design decisions, credentials, or waiting on the
owner/bench). No new item to file. `DELTAMINT_OPS_TOKEN` still not set
(checked directly) — `opsHealth` not called on either project, same standing
consequence as every run today.

**What is up.** `https://deltamint.app`, `/blog`, `/pricing` and
`https://dashboard.deltamint.app` all re-fetched directly: four `CONNECT
tunnel failed, response 403` at the egress proxy, identical to every run
today and to the standing row in `docs/context/reachable.md`. No new row;
not reported as down.

**Checks on staging**, fresh container (`node_modules` absent; `npm ci`
clean), all green at `753e3d3`:
- `npm run lint` — clean.
- `npm test` — 407 client + 625 server, 1,032 pass, 0 fail.
- `npm run build` — clean; same pre-existing >500 kB chunk-size warning, not
  new.
- `npm run context:check` — `docs/product-context.md is current.`

**Queue drain.** Checked both open duty-engineer PRs via the GitHub MCP
tools: [#11](https://github.com/OsamaMaghawry/Spreads/pull/11) (blog
diagrams, `duty/2026-09-21-blog-diagrams` → `main`) and
[#9](https://github.com/OsamaMaghawry/Spreads/pull/9) (`staging` → `main`,
migration confirmation) are both still open, correctly not merged, unchanged
since 16:06. No new ticket to open or close.

**Fixed this run:** nothing — no failing code found, checks were already
green going in.

**Escalated:** nothing new.

**PR:** none opened — no application code changed and nothing new to
escalate.

**Email:** not sent — nothing new is money-path or user-visible this run.
Clean hour, ledger line only.
