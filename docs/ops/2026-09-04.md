# 2026-09-04

## 13:12 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`, now at `d1c3262`).

**What changed.** `git log --oneline` since the last ledger entry (the
2026-09-03 20:07 run, at `65b5d38`/`7b9e537`) on both `main` and `staging`
turned up seven commits touching `src/`, `supabase/functions/` or
`landing/` not yet in `shipped.md`: `cc855e7` and `b453a9f` (Hotjar session
replay on the four public landing pages, hostname-gated like the GA tag,
plus the privacy-policy disclosure compliance-gate asked for) and, on
09-04, `326ce67`, `f0710c8`, `08b1fe5`, `fc9598f`, `d1c3262` (a share
position can close from the same ticket as an option, the price walk can
be left without trapping the order, a repriced resting order updates
itself instead of duplicating, shares behind a working sell order no
longer read as free to sell, the orders row stops calling a share order a
"leg", and the position watch stops emailing repeated identical lines for
one price problem). All are on `main` already via `21543c0` and `1fa3d40`
— no `(staging)` tag needed. Added six lines to `docs/ops/shipped.md`
(grouping the closely-related share/orders commits into two lines) and
regenerated `docs/product-context.md` with `npm run context`.
`54187d1` (metrics snapshot) touches neither path — no ledger line.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called on either project; existing `needs owner` ticket,
not duplicated. `docs/ops/queue.md` re-read: 10 `needs owner`, 2
`escalated`, 0 `open`, 2 `fixed`. Both escalated items re-checked against
current code — `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — unchanged, no
new evidence, not duplicated. `open` section empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md` and the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** `npm install` (clean). `npm run lint` clean. `npm test` —
192/192 passing (up from 189 at the last run, three new tests shipped with
09-04's commits). `npm run build` clean. `npm run context:check` initially
failed (`shipped.md` had grown since the last regeneration) — `npm run
context` regenerated `docs/product-context.md`, then `context:check`
passed clean.

**Fixed:** nothing — no new plain bug found; the `context:check` failure
was closed by regenerating the derived doc, not a code fix.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this run;
the standing escalations and the consent-banner `needs owner` item are
unchanged findings already in the queue.

## 14:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, now at
`228fea9`).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up one commit not yet in `shipped.md`: `228fea9` — the scanner now
tells "the options market is shut for the day" apart from "the stock and
options feeds disagree": pre/post-market the parity-guard skip reads
"Options closed until 09:30 ET" instead of blaming a data fault, and the
open-position scan loop backs off to a 60s retry outside market hours
instead of hammering the dead chain every 20s. Added one line to
`docs/ops/shipped.md` and regenerated `docs/product-context.md` with `npm
run context`.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 10 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call
(`closeSpread/index.ts` still the only one that writes `order_attempts`
rows), `oauthDiag/index.ts` is still gated only by `requireUser`, no
`adminData`-style admin check — both unchanged, no new evidence, not
duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated.

**The checks.** `npm install` (611 packages; `eslint.config.js` could not
resolve `globals` before this — the install fixed it, not a repo bug).
`npm run lint` clean. `npm test` — 193/193 passing (up from 192 at the
13:12 run — `228fea9` shipped `marketSession.test.js` plus new
`optionScan.test.ts` cases). `npm run build` clean. `npm run
context:check` initially failed (`shipped.md` had grown since the last
regeneration) — `npm run context` regenerated `docs/product-context.md`,
then `context:check` passed clean.

**Fixed:** nothing — no new plain bug found; `228fea9` was already on
`staging` from an earlier session, only its ledger line was missing.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 15:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, still at
`75c2566`).

**What changed.** `git log --since="26 hours ago" --oneline origin/main
origin/staging -- src supabase/functions landing` turned up nothing past
the 14:07 run — `origin/main` moved from `4f65503` to `1fa3d40`, but that
commit is only the "Ship 4 September" merge of work already logged in
`shipped.md`. No new line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 10 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — both
unchanged, no new evidence, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated.

**The checks.** `npm install` (fresh container; 611 packages, no repo
issue). `npm run lint` clean. `npm test` — 193/193 passing. `npm run
build` clean. `npm run context:check` passed clean on the first try — no
regeneration needed this run.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 16:06 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, at
`daf458c`).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up nothing past the 15:07 run — `staging` is still at `daf458c`,
`origin/main` unchanged at `1fa3d40`. No new `shipped.md` line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 10 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — both
unchanged, no new evidence, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated.

**The checks.** `npm install` (fresh container; 611 packages — fixes the
`eslint.config.js` "Cannot find package 'globals'" error seen before
install, not a repo bug). `npm run lint` clean. `npm test` — 193/193
passing. `npm run build` clean. `npm run context:check` passed clean on
the first try — no regeneration needed this run.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 17:06 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, at
`b3dd467`).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up nothing past the 16:06 run — `staging` unchanged at `b3dd467`,
`origin/main` unchanged at `1fa3d40`. No new `shipped.md` line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 10 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — both
unchanged, no new evidence, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated.

**The checks.** `npm install` (fresh container; 611 packages, no repo
issue). `npm run lint` clean. `npm test` — 193/193 passing. `npm run
build` clean. `npm run context:check` passed clean on the first try — no
regeneration needed this run.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 18:05 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, at `0178527`).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up nothing past the 17:06 run — `staging` unchanged at `0178527`,
`origin/main` unchanged at `1fa3d40`. No new `shipped.md` line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 10 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — both
unchanged, no new evidence, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated.

**The checks.** `npm install` (fresh container; 611 packages, no repo
issue). `npm run lint` clean. `npm test` — 193/193 passing. `npm run
build` clean. `npm run context:check` passed clean on the first try — no
regeneration needed this run.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 19:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`. `git fetch origin`, then `git checkout
staging` (clean checkout, up to date with `origin/staging`, at `bab3588`).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up nothing past the 18:05 run — `staging` unchanged at `bab3588`,
`origin/main` unchanged at `1fa3d40`. No new `shipped.md` line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
confirmed with an env check (not printed) — so `opsHealth` was not called
on either project; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 11 `needs owner`, 2 `escalated`, 0 `open`, 2
`fixed` — nothing to drain. Both standing escalations re-checked against
current code: `openPosition/index.ts` still has no `recordAttempt` call,
`oauthDiag/index.ts` is still gated only by `requireUser` — both
unchanged, no new evidence, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked-hosts list in `docs/ops/access.md`; not
re-reported as "down", not duplicated. `docs/context/reachable.md`'s
`Ours` row was stale (still read `✅ 200, 2026-08-31`) — updated it to the
current blocked status with today's re-test, so the register reflects
what a session can see now.

**The checks.** `npm ci` (fresh container; 611 packages — fixes the
`eslint.config.js` "Cannot find package 'globals'" error seen before
install, not a repo bug). `npm run lint` clean. `npm test` — 193/193
passing. `npm run build` clean. `npm run context:check` passed clean on
the first try — no regeneration needed this run.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this
run; the standing escalations and the consent-banner `needs owner` item
are unchanged findings already in the queue.

## 20:05 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`,
`docs/ops/shipped.md`, `docs/context/reachable.md`. On `staging`, clean
tree, up to date with `origin/staging` at `4859da4`.

**What changed.** `git log --since="26 hours ago" --oneline main staging`:
nothing past the 19:07 run. The newest code commit in the window is still
`228fea9` (13:31 UTC, the market-session scanner message), already in
`shipped.md`; everything since is ops-ledger only. No new `shipped.md`
line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` is still not set in this environment
(checked without printing it), so `opsHealth` was not called on either
project — order errors, unfilled walks, open alerts and broker connection
issues are unreadable from here for a third consecutive day. Standing
`needs owner` ticket, not duplicated. `docs/ops/queue.md` re-read: 11
`needs owner`, 2 `escalated`, 0 `open`, 2 `fixed` — nothing to drain. Both
standing escalations re-verified against current code and unchanged:
`supabase/functions/openPosition/index.ts` contains zero `recordAttempt`
references, and `supabase/functions/oauthDiag/index.ts` is still gated
only by `requireUser` (line 31) with no admin check.

**What is up.** All four hosts refused at the proxy —
`deltamint.app`, `/blog`, `/pricing` and `dashboard.deltamint.app` each
returned `CONNECT tunnel failed, response 403`, the allowlist failure mode,
before the origin was ever asked. Not reported as down. `reachable.md`'s
`Ours` row already carries today's re-test from the 19:07 run, so no edit
was needed. `X-Robots-Tag` on the dashboard could not be observed for the
same reason.

**The checks.** `npm ci` first (fresh container — `npm run lint` fails with
`Cannot find package 'globals'` before install; environment, not a repo
bug). Then `npm run lint` clean, `npm test` 193/193 passing, `npm run
build` clean, `npm run context:check` reports `docs/product-context.md is
current`. All green on `staging`.

**Extra sweep (no findings).** Because the hour was otherwise idle, ran two
read-only defect scans for the class this role fixes:

- `node --experimental-strip-types --check` over every
  `supabase/functions/*/index.ts` and `_shared/*.ts`. Four files report
  `SyntaxError: Unexpected token ':'` — `billingEvents.ts`, `crypto.ts`,
  `orderAttempts.ts`, `wheelBasis.ts` — and all four are the same Node
  22 artefact, not a defect: those modules have no top-level `import`, so
  `--check` classifies them as CommonJS and skips type stripping. Each is
  imported and exercised by its own passing test through an ESM entry
  point. Recorded so a future run does not chase it. No CI step runs this
  check; nothing to fix.
- `no-undef` over every file in `src/`, `worker/`, `landing/src/` and
  `scripts/` with a throwaway flat config, because `eslint.config.js`
  deliberately scopes the real run to `src/components`, `src/pages` and
  `src/Layout.jsx` and ignores `src/lib/**` — the blind spot where the
  `step`/`sharesByTicker` class of bug has shipped before. Zero
  undefined identifiers anywhere. The only three errors reported were my
  ad-hoc config lacking `react-hooks`, which the repo config supplies.

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new.

**No email sent.** Nothing new on the money path or user-visible this run;
the standing escalations and the consent-banner `needs owner` item are
unchanged findings already in the queue.
