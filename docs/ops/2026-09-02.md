# 2026-09-02

## 14:08 UTC run (first duty-engineer run — no prior ledger exists)

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
(none of the three exist on `main`; all three live on `staging`, which is
where this run happened). `git log` on `main` and `staging`, `docs/ops/queue.md`,
`docs/context/reachable.md`, `docs/context/brand.md`.

**What changed.** `staging` is far ahead of `main` (`main` is at `4f65503`;
`staging` has 20+ additional commits, including the billing, scanner-wheel,
pricing and duty-engineer work). `docs/ops/shipped.md` was missing lines for
five commits that touch `src/`, `supabase/functions/` or `landing/` — added:
the nav-label fix (below), the naked-call-vs-covered-spread fix (`616c203`),
the blog-categories/daily-article/RSS work and the Engagement KPI panel
(`de4f8d6`, two lines), and the pricing-page correction (`e982774`). `f8c427b`
(scanner: CSP/CC/wheel) already had a line.

**What broke.** `DELTAMINT_OPS_TOKEN` is not set in this environment, so the
`opsHealth` endpoint was not called — read what I could from code and
`docs/ops/queue.md` instead (already recorded as `needs owner`, not
duplicated). No new "what broke" tickets from this reading.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) refused the CONNECT at the proxy (403) — the
existing `needs owner` allowlist ticket already covers this; not re-reported
as "down", not duplicated.

**The checks.** `node_modules` was effectively empty (`npm install` needed
first — a fresh checkout, not a break). After that: `npm run lint` clean,
`npm test` — 177/177 passing, `npm run build` clean, `npm run context:check`
— `docs/product-context.md` is current. All green.

**Fixed:**
- `src/components/Layout.jsx` — the nav's first link was labelled
  "dashboard"; `docs/context/brand.md` names that word forbidden (canonical:
  "Positions Monitor", which is what the page's own H1 says). Changed the
  label to "positions", matching the single-word style of the other tabs
  (`screener`, `accounts`, `billing`, `admin`). Lint and build re-verified
  green; no component-test harness exists in this repo to unit-test a JSX
  label (`npm test` only runs `node --test` over `src/lib/*.test.js`,
  `worker/`, `landing/src/` and `supabase/functions/_shared/*.test.ts` —
  nothing compiles JSX), so this shipped on lint+build confirmation only.
  Commit: see below.

**Escalated** (written to `docs/ops/queue.md`, evidence and proposed patches
there):
- `oauthDiag` is reachable by any signed-in user (no admin gate). Not a
  credential leak — the Alpaca client secret is never returned — but any
  signed-in user can trigger a live Alpaca OAuth exchange with our app's
  credentials. Touches OAuth credentials, outside plain-bug authority;
  routed to systems-engineer per the existing ticket. Not money-path or
  user-visible in a way that needed a founder email this run.
- `openPosition` never writes an `order_attempts` row (`closeSpread` writes
  one on every close, success or failure). Proposed patch mirrors
  `closeSpread`'s existing, already-tested `recordAttempt` pattern exactly —
  no new dependency or schema — but it touches the order-submission path and
  no test harness exists for either function's `Deno.serve` handler, so it
  goes to the queue rather than getting fixed inline.
- Moved "confirm `0024_broker_feed_dumps` applied on production" from `open`
  to `needs owner` — duty-engineer has no production database access and
  never touches production, so this can only be confirmed by someone who
  does.

**No email sent.** Nothing found this run was on the money path or
user-visible in a way that needed the founder before the next run; all
checks are green on `staging`.

## 15:09 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
(same as the 14:08 run — all three still absent from `main`, present on
`staging`, which is where this run happened too). `docs/ops/queue.md`,
`docs/context/reachable.md`.

**What changed.** `git log --since="26 hours ago"` on `staging` shows only
the two housekeeping commits the last run made after writing its own ledger
(`a1b9efa` back-filling the nav-fix commit hash, `7cd551b` regenerating
`docs/product-context.md`) — both already reflected in `docs/ops/shipped.md`
and the 14:08 ledger entry above. `main` has no commits in the window (still
`4f65503`). Nothing new to append to `shipped.md`.

**What broke.** `DELTAMINT_OPS_TOKEN` is still not set — `opsHealth` not
called; recorded, not duplicated (existing `needs owner` ticket covers it).
`docs/ops/queue.md` unchanged since the last run: 8 `needs owner`, 2
`escalated` (`oauthDiag` admin-gating, `openPosition` missing
`recordAttempt`), 0 `open`, 2 `fixed`. No new evidence to add to either
escalated item this run.

**What is up.** All four hosts refused the CONNECT at the proxy (403),
same as the 14:08 run — the existing `needs owner` allowlist ticket already
covers this; not re-reported as "down", not duplicated.

**The checks.** Fresh checkout again (`node_modules` had only 1 entry) —
`npm install` first (611 packages, not a break). After that: `npm run lint`
clean, `npm test` — 177/177 passing, `npm run build` clean, `npm run
context:check` — `docs/product-context.md` still current. All green.

**Fixed:** none — nothing new to reproduce.

**Escalated:** none new — the queue's `open` section is empty, so there was
nothing to drain this run.

**No email sent.** Nothing new to escalate, no red check on staging.
