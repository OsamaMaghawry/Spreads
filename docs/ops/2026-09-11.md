# 2026-09-11

## 13:09 UTC — duty-engineer run

**Read.** `git log --since="26 hours ago" --oneline main staging` (main and
staging both at `4bfbf45`/`b6d26bf` — staging is ahead). Four commits since
the last run touched `src/` or `supabase/functions/` and were not yet in
`docs/ops/shipped.md`: `0cde265` (open option legs missing from Account
Analysis), `a49da5e` (correction to that commit's own description — the code
was right, the illustration in the message was stale broker data presented as
current), `9faf020` (the bench's STOP on the daily-equity chart: an undatable
leg could store a live book as a complete $0.00 day, plus backdated cost
basis, unbounded/unordered history reads, and a caught pricing error that
silently zeroed the options book), and `b6d26bf` (trade history now syncs on
a schedule instead of only when a user opens the page). Appended plain-English
lines for all four to `shipped.md`.

`docs/ops/queue.md` unchanged since `d9eab87` (last run, 2026-09-10 13:08) —
every open item is already in its correct state (needs owner / escalated /
open and owned by systems-engineer or head-of-trading's leg-first rebuild
pass). Nothing new to drain this run.

`DELTAMINT_OPS_TOKEN` is not set in this environment — opsHealth not called;
read what's available from code and the site instead, per the brief.

`https://deltamint.app`, `/blog`, `/pricing` and `https://dashboard.deltamint.app`
re-tested: all four still 403 at CONNECT, same failure mode already recorded
in `docs/context/reachable.md`. Unchanged from the existing row — not
reporting "down", the hosts were not reachable to test.

**Checks on staging.** `node_modules` was not installed in this fresh
container (`npm run lint` failed on `Cannot find package 'globals'`, not a
code defect) — ran `npm ci` first. After that:
- `npm run lint` — clean.
- `npm test` — 195 + 405 = 600 tests, all green.
- `npm run build` — clean (one pre-existing chunk-size warning, not new).
- `npm run context:check` — **red**: `docs/product-context.md` was stale
  against the `shipped.md` entries just added. This is the mechanical,
  fully-derived doc the script regenerates from tracked files (no git
  history, no judgment involved) — ran `npm run context`, diff was exactly
  the expected 3-line window shift reflecting the new shipped entries, no
  other change. Fixed, not escalated: a CI check red for a reason inside
  this run's own diff.

**Fixed this run:** `shipped.md` ledger catch-up (4 entries) +
`product-context.md` regeneration to match, commit `c7d6a65`.

**Escalated:** nothing new.

**Email:** not sent — no new escalation and no check left red.

**No PR opened to `main`.** `staging` is dozens of commits ahead of `main`
(the leg-first rebuild, billing, the Broker tab, the daily-equity feature,
and more, none of it merged yet), and both fixes this run were ops
bookkeeping — the shipped ledger and its fully-derived summary doc — not an
isolated application bug. A `staging → main` PR would bundle all of that
unreviewed, unrelated work in with it, which is the owner's own release-gate
call (`docs/context/org.md`: staging → verify on real data → main, every
time), not duty-engineer's to force by opening the PR. Consistent with past
runs: substantive bug fixes get their own hotfix branch and PR (e.g.
`af75776`/`hotfix/watch-long-calls`); ledger-only clean hours stay on
`staging`. Checked for an existing open PR into `main` first — none exists.

Pushed to `staging` (`e9b027f`).

## 14:07 UTC — duty-engineer run

**Read.** `git log f674e38..HEAD --oneline main staging` — empty. No commits
have landed on either branch since the last run (`f674e38`, 13:10 UTC);
`shipped.md` needs no new entries.

`docs/ops/queue.md` unchanged since `d9eab87` (2026-09-10 13:08 UTC) — every
open item is still in its correct state (needs owner / escalated / open and
owned by systems-engineer or head-of-trading's leg-first rebuild pass, or the
owner's own Search Console findings). Nothing new to drain this run.

`DELTAMINT_OPS_TOKEN` is not set in this environment — opsHealth not called;
read what's available from code and the site instead, per the brief.

`https://deltamint.app`, `/blog`, `/pricing` and `https://dashboard.deltamint.app`
re-tested: all four still 403 at CONNECT, same failure mode already recorded
in `docs/context/reachable.md`. Unchanged from the existing row — not
reporting "down", the hosts were not reachable to test.

**Checks on staging.** Fresh container again (`node_modules` not installed);
ran `npm ci` first, then:
- `npm run lint` — clean.
- `npm test` — 195 + 405 = 600 tests, all green.
- `npm run build` — clean (same pre-existing chunk-size warning, not new).
- `npm run context:check` — clean, `docs/product-context.md` is current.

**Fixed this run:** nothing — no new commits, no red check.

**Escalated:** nothing new.

**Email:** not sent — no new escalation and no check left red. Clean hour.

No push needed — nothing changed.
