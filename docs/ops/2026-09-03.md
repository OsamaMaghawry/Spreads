# 2026-09-03

## 13:13 UTC run (first duty-engineer run of the day — no prior ledger for today)

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then worked on `staging` (checked out cleanly this run).

**What changed.** `git log` on `origin/main` and `origin/staging` since the
last ledger commit (`43d537e`, the 2026-09-02 20:05 run). Both branches
picked up the same batch of `src/`/`supabase/functions/` commits overnight
(03:xx–04:xx UTC): `f6c481f` + `d650266` (an admin draft-preview feature,
the second commit reworking the first into its own untabbed page at
`/blog-preview/<slug>`) and `b85c13b` + `20581e9` (a `billing_visible`
switch that hides the whole payment surface — nav entry, `/billing`,
checkout, portal — until the owner flips it, since Alpaca hasn't approved
live trading yet). Neither had a `shipped.md` line yet; added one each
(dated 2026-09-03, above the 09-02 entries). `main` additionally carries
`fbed254` ("Ship 2 September", a squash of the day's staging work — no new
user-facing content beyond what `shipped.md` already lines) and `58a1a1b`
(a growth metrics snapshot, `docs/growth/metrics/` only — not `src/`,
`supabase/functions/` or `landing/`, no ledger line needed). The
`25ce40b`/`63740d9`/`53f1e27`/`702e9fa` commits (blog content + the
content-check script) are content-cadence work, not `src/`/`supabase/functions/`/`landing/`
— outside this ledger's scope, left to `shipped.md`'s existing convention.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called; already an existing `needs owner` ticket, not
duplicated. `docs/ops/queue.md`: 9 `needs owner`, 2 `escalated` (`oauthDiag`
admin-gating, `openPosition` missing `recordAttempt`), 0 `open`, 2 `fixed`.
Re-checked both escalated items against current code: `oauthDiag/index.ts`
still gates on `requireUser` only, no admin check; `openPosition/index.ts`
still has no `recordAttempt` import or call, while `closeSpread/index.ts`
still writes one on every leg. Neither touched since the last check — no
new evidence to add. Queue's `open` section is empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) returned 403 at the proxy CONNECT — the existing
`needs owner` allowlist ticket already covers this; not re-reported as
"down", not duplicated.

**The checks.** Fresh checkout (`node_modules` empty) — `npm install` first
(611 packages, not a break). `npm run lint` clean. `npm test` — 189/189
passing (same count as the last 09-02 runs — no test-affecting commits in
the window). `npm run build` clean. `npm run context:check` — **red**:
`docs/product-context.md` was stale (missing the two new `shipped.md`
lines added this run). Fixed per the brief's "a CI check that is red for a
reason inside the diff": ran `npm run context` to regenerate it (adds the
two new lines, drops the two oldest from the fixed-length recent-shipped
window — the auto-generated file's normal behavior, not a content loss —
the lines stay in `shipped.md` itself), then re-ran the full chain
(`lint && test && build && context:check`) clean.

**Fixed:**
- `docs/product-context.md` regenerated via `npm run context` so
  `context:check` is green again, after `shipped.md` gained two new lines
  this run. No reproducing test needed — the check itself is the
  reproduction, and it now passes. Commit below.

**Escalated:** none new — nothing in the queue's `open` section, and no
new plain bug found this run.

**No email sent.** Nothing found this run was on the money path or
user-visible in a way that needed the founder before the next run; all
checks are green on `staging`.

## 14:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline 4f79dab..origin/main` (since the last
ledger commit, the 13:13 run) shows only `fbed254` and `58a1a1b`, both
already accounted for in that run's ledger entry (the "Ship 2 September"
squash and the metrics-only snapshot) — no new `shipped.md` line needed.
`git log --oneline 4f79dab..origin/staging` is empty: no new commits on
`staging` since the last run.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set — `opsHealth` not
called; existing `needs owner` ticket, not duplicated. `docs/ops/queue.md`
re-read: 9 `needs owner`, 2 `escalated`, 0 `open`, 2 `fixed`. Re-checked
both escalated items against current code: `oauthDiag/index.ts` still gates
on `requireUser` only (no admin check); `openPosition/index.ts` still has
no `recordAttempt` import or call, while `closeSpread/index.ts` still
writes one on every leg (`recordAttempt` at lines 85/88/129/132). Neither
changed since the last check — no new evidence to add. `open` section
still empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** Fresh checkout (`node_modules` empty again) — `npm install`
first (611 packages). `npm run lint` clean. `npm test` — 189/189 passing
(same count as the last run). `npm run build` clean. `npm run
context:check` — clean, no diff to regenerate this time (nothing new in
`shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.

## 15:08 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline 6d6a631..origin/main` (since the last
ledger commit, the 14:07 run) shows `58a1a1b` (metrics snapshot) and
`fbed254`/`e672753`/`c680972`/`b12296d`, all already accounted for in
earlier ledger entries — no new `shipped.md` line needed. `git log
--oneline 6d6a631..origin/staging` is empty: no new commits on `staging`
since the last run.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set — `opsHealth` not
called; existing `needs owner` ticket, not duplicated. `docs/ops/queue.md`
re-read: 9 `needs owner`, 2 `escalated`, 0 `open`, 2 `fixed`. Re-checked
both escalated items against current code: `oauthDiag/index.ts` still
gates only on `requireUser` (line 31), no admin check; `openPosition/index.ts`
still has no `recordAttempt` import or call, while `closeSpread/index.ts`
still writes one on every leg (lines 85/88/129/132). Neither changed since
the last check — no new evidence to add. `open` section still empty —
nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** Fresh checkout (`node_modules` empty again) — `npm install`
first. `npm run lint` clean. `npm test` — 189/189 passing (same count as
the last run). `npm run build` clean. `npm run context:check` — clean, no
diff to regenerate (nothing new in `shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.

## 16:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline c0b174c..origin/main` and
`c0b174c..origin/staging` (since the last ledger commit, the 15:08 run):
`origin/main` shows nothing new (still at `58a1a1b`, already accounted for
in the 14:07 run's entry); `origin/staging` is empty — no new commits on
either branch. No new `shipped.md` line needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 9 `needs owner`, 2 `escalated`, 0 `open`,
2 `fixed`. Re-checked both escalated items against current code:
`oauthDiag/index.ts` still gates only on `requireUser` (line 31), no admin
check; `openPosition/index.ts` still has no `recordAttempt` import or call.
Neither changed since the last check — no new evidence to add. `open`
section still empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** Fresh checkout (`node_modules` empty again) — `npm install`
first (611 packages). `npm run lint` clean. `npm test` — 189/189 passing
(same count as the last run). `npm run build` clean. `npm run
context:check` — clean, no diff to regenerate (nothing new in `shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.

## 17:06 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline d396da0..origin/main` (since the last
ledger commit, the 16:07 run) shows `58a1a1b`, `fbed254`, `e672753`,
`c680972`, `b12296d` — all already accounted for in earlier ledger entries
(the metrics snapshot, the "Ship 2 September" squash, the naked-call watch
fix and its merge). `git log --oneline d396da0..origin/staging` is empty:
no new commits on `staging` since the last run. No new `shipped.md` line
needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 9 `needs owner`, 2 `escalated`, 0 `open`,
2 `fixed`. Re-checked both escalated items against current code:
`oauthDiag/index.ts` still gates only on `requireUser` (line 31), no admin
check; `openPosition/index.ts` still has no `recordAttempt` import or call,
while `closeSpread/index.ts` still writes one on every leg (lines
85/88/129/132). Neither changed since the last check — no new evidence to
add. `open` section still empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** Fresh checkout (`node_modules` empty again) — `npm install`
first (611 packages). `npm run lint` clean. `npm test` — 189/189 passing
(same count as the last run). `npm run build` clean. `npm run
context:check` — clean, no diff to regenerate (nothing new in `shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.

## 18:05 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline fa4adc3..origin/main` (since the last
ledger commit, the 17:06 run) shows `58a1a1b`, `fbed254`, `e672753`,
`c680972`, `b12296d` — all already accounted for in earlier ledger entries
(the metrics snapshot, the "Ship 2 September" squash, the naked-call watch
fix and its merge). `git log --oneline fa4adc3..origin/staging` is empty:
no new commits on `staging` since the last run. No new `shipped.md` line
needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 9 `needs owner`, 2 `escalated`, 0 `open`,
2 `fixed`. Re-checked both escalated items against current code:
`oauthDiag/index.ts` still gates only on `requireUser`, no admin check;
`openPosition/index.ts` still has no `recordAttempt` import or call, while
`closeSpread/index.ts` still writes one on every leg (lines 85/88/129/132).
Neither changed since the last check — no new evidence to add. `open`
section still empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** `node_modules` present — `npm install` re-run anyway,
clean. `npm run lint` clean. `npm test` — 189/189 passing (same count as
the last run). `npm run build` clean. `npm run context:check` — clean, no
diff to regenerate (nothing new in `shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.

## 19:05 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`).

**What changed.** `git log --oneline a01553c..origin/main` (since the last
ledger commit, the 18:05 run) shows `58a1a1b`, `fbed254`, `e672753`,
`c680972`, `b12296d` — all already accounted for in earlier ledger entries
(the metrics snapshot, the "Ship 2 September" squash, the naked-call watch
fix and its merge). `git log --oneline a01553c..origin/staging` is empty:
no new commits on `staging` since the last run. No new `shipped.md` line
needed.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment —
`opsHealth` not called; existing `needs owner` ticket, not duplicated.
`docs/ops/queue.md` re-read: 9 `needs owner`, 2 `escalated`, 0 `open`,
2 `fixed`. Re-checked both escalated items against current code:
`oauthDiag/index.ts` still gates only on `requireUser` (line 31), no admin
check; `openPosition/index.ts` still has no `recordAttempt` import or call.
Neither changed since the last check — no new evidence to add. `open`
section still empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — CONNECT tunnel failed, 403, same as the
existing `needs owner` allowlist ticket; not re-reported as "down", not
duplicated.

**The checks.** `npm install` (611 packages). `npm run lint` clean. `npm
test` — 189/189 passing (same count as the last run). `npm run build`
clean. `npm run context:check` — clean, no diff to regenerate (nothing new
in `shipped.md`).

**Fixed:** nothing — all checks were already green.

**Escalated:** none new — `open` section empty, no new plain bug found.

**No email sent.** Nothing new this run; all checks green on `staging`;
both standing escalations unchanged since the last check.
