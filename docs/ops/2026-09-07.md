# 2026-09-07

## 13:xx UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`.
`git fetch origin`, then `git checkout staging` (clean checkout, up to date
with `origin/staging`, at `204b853`). No ledger file existed for this date —
the prior entry is 2026-09-04 20:05 UTC; the gap between is a scheduling
gap, not this run's to explain.

**What changed.** `git log --since="2026-09-04 20:05" --oneline main staging`
turned up three commits, all touching `landing/`: `f55ef60` (new homepage
built from the product itself — hero wipe between broker rows and DeltaMint
cards, looping screener/ticket replays, Analysis tiles, FAQ, close), `fa8726c`
(drop the app's top nav from the replicas, more air above the hero) and
`204b853` (smaller broker logo in the connect card). `git diff origin/main
origin/staging -- landing/` is empty — already on `main` (as `342b9b7`'s
neighbours `f14570a`/`0c423ee`/`c3675a3`), no `(staging)` tag needed. Added
one grouped line to `docs/ops/shipped.md`.

While checking `git diff origin/main origin/staging` more broadly (to confirm
nothing else was staging-only), found `src/lib/marketSession.js` present on
`staging` and entirely absent from `main`, along with staging-only changes to
`useScanLoop.js`, `optionScan.ts`, `findEntry/index.ts` and
`scanEntries/index.ts` — all from `228fea9` (2026-09-04, the scanner
market-hours message). `git merge-base --is-ancestor 228fea9 origin/main`
fails and no open PR exists for it (`list_pull_requests` state=open: empty).
Three ledger entries (09-04's 14:07/15:07/16:06 runs) treated `228fea9` as
shipped without a `(staging)` tag. Corrected the `shipped.md` line to add
`(staging)` and a note that it's not on `main`, and opened a `needs owner`
ticket in `docs/ops/queue.md` — not a duty-engineer fix (no bug, and not
this run's change), just a three-day-old gap the owner should know about:
either merge it or confirm it's being held back deliberately.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment
(checked without printing it) — `opsHealth` not called on either project.
Standing `needs owner` ticket, not duplicated. `docs/ops/queue.md` re-read:
12 `needs owner` (now, after the new ticket above), 2 `escalated`, 0 `open`,
2 `fixed`. Both standing escalations re-verified against current code:
`supabase/functions/openPosition/index.ts` still has zero `recordAttempt`
references; `supabase/functions/oauthDiag/index.ts` is still gated only by
`requireUser`, no admin check — both unchanged, no new evidence, not
duplicated. `open` section empty — nothing to drain.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`, matching
the known-blocked-hosts list in `docs/ops/access.md`; not reported as down,
not duplicated.

**The checks.** `npm ci` (fresh container; 611 packages, no repo issue).
`npm run lint` clean. `npm test` — 193/193 passing. `npm run build` clean.
`npm run context:check` initially would have gone stale once `shipped.md`
and `queue.md` were edited — ran `npm run context` to regenerate
`docs/product-context.md`, then `context:check` passed clean. All green on
`staging` before any ledger edits, confirming the pre-existing state was
healthy; the edits made this run are docs-only (`shipped.md`, `queue.md`,
`docs/product-context.md`, this file).

**Fixed:** nothing — no new plain bug found.

**Escalated:** none new (the `228fea9`/no-PR gap is a `needs owner` ticket,
not an escalation — it isn't a bug, a number-meaning change, or inside
duty-engineer's fix authority either way).

**No email sent.** Nothing on the money path or user-visible this run: the
new ticket concerns a staging-only fix that has shipped to no one yet, and
the standing escalations and consent-banner `needs owner` item are
unchanged findings already in the queue.

## 14:08 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`.
`git fetch origin`, `git checkout staging` (clean, up to date with
`origin/staging`, at `b6c209d` — the previous run's own ledger commit).

**What changed.** `git log --since="2026-09-07 13:16:16" --oneline main
staging` (the previous run's commit time) returns only `b6c209d` itself —
no new commits on either branch. `git diff origin/main origin/staging
--stat` shows the same staging-only files as last run
(`marketSession.js`, `useScanLoop.js`, `optionScan.ts`, `findEntry`,
`scanEntries`, all from `228fea9`) plus this run's own not-yet-pushed
ledger/doc edits — nothing new to add to `shipped.md`.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set (checked without
printing it) — `opsHealth` not called on either project, same as every
prior run. `docs/ops/queue.md` re-read: `open` section empty, nothing to
drain. Re-verified both standing escalations against current code, no
drift: `supabase/functions/openPosition/index.ts` still has zero
`recordAttempt` references (`grep` empty); `supabase/functions/oauthDiag/index.ts`
still gated only by `requireUser` (line 31), no admin check. Re-verified
the `228fea9` gap: `git merge-base --is-ancestor 228fea9 origin/main`
still fails — still no PR, still a `needs owner` item, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked list in `docs/ops/access.md`; not reported as
down.

**The checks.** Fresh container, `npm ci` first (611 packages, clean).
`npm run lint` clean. `npm test` — 193/193 passing. `npm run build`
clean. `npm run context:check` passed clean with no regeneration needed
(no source changes this run).

**Fixed:** nothing — no new plain bug found, nothing in the queue to
drain.

**Escalated:** none new — no findings this run.

**No email sent.** Nothing on the money path or user-visible this run;
all standing `needs owner`/`escalated` items are unchanged from the
previous run's hour.

## 15:08 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`.
`git fetch origin`, `git checkout staging` (clean, up to date with
`origin/staging`, at `cbc2e21` — the previous run's own ledger commit).

**What changed.** `git log --since="2026-09-07T14:08:00Z" --oneline main
staging` (the previous run's commit time) returns only `cbc2e21` itself —
no new commits on either branch. Nothing to add to `shipped.md`.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set (checked without
printing it) — `opsHealth` not called on either project, same as every
prior run. `docs/ops/queue.md` re-read: `open` section empty, nothing to
drain. Re-verified both standing escalations against current code, no
drift: `supabase/functions/openPosition/index.ts` still has zero
`recordAttempt` references; `supabase/functions/oauthDiag/index.ts` still
gated only by `requireUser` (line 31), no admin check. Re-verified the
`228fea9` gap: `git diff origin/main origin/staging --stat` shows the same
staging-only files as every run since 09-04 (`marketSession.js`,
`useScanLoop.js`, `optionScan.ts`, `findEntry`, `scanEntries`) — still a
`needs owner` item, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked list in `docs/ops/access.md`; not reported as
down.

**The checks.** Fresh container, `npm ci` first (611 packages, clean).
`npm run lint` clean. `npm test` — 193/193 passing. `npm run build`
clean. `npm run context:check` passed clean with no regeneration needed
(no source changes this run).

**Fixed:** nothing — no new plain bug found, nothing in the queue to
drain.

**Escalated:** none new — no findings this run.

**No email sent.** Nothing on the money path or user-visible this run;
all standing `needs owner`/`escalated` items are unchanged from the
previous run's hour.

## 16:09 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`.
`git fetch origin`, `git checkout staging` (clean, up to date with
`origin/staging`, at `0053b90`).

**What changed.** `git log --since="2026-09-07T15:08:00Z" --oneline main
staging` turns up one new commit: `0053b90` (staging only — not on `main`),
"Content goes to staging for review, and stops being refused for its
diagrams." Touches `.github/workflows/content-merge.yml`, adds
`publish-blog-staging.yml`, a blog post and two diagram SVGs under
`landing/public/assets/blog/`, and a small change to
`scripts/email-digest.mjs` — the content merge gate now counts a post's own
diagrams as part of the post (previously any post with an image read as
"not content-only" and sat unmerged, which is why five days of drafted
posts never reached anywhere a person could read them), and it lands the
branch on `staging` instead of `main` so the owner can read a draft at
`dev-landing.deltamint.app/blog` before it goes live; a merge from staging
to main stays the owner's own call. Added a line to `docs/ops/shipped.md`.
The commit's own message flags it needs one secret,
`SUPABASE_SERVICE_ROLE_KEY_STAGING`, to run — confirmed in
`publish-blog-staging.yml` lines 46-50 (the job checks for it and refuses
loudly rather than half-publishing) — not yet in `docs/ops/queue.md`
(the existing `SUPABASE_SERVICE_ROLE_KEY` ticket from 2026-09-02 is the
**production** key, for a different pair of workflows). Filed a new
`needs owner` ticket for it.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set (checked without
printing it) — `opsHealth` not called on either project, same as every
prior run. `docs/ops/queue.md` re-read: `open` section still empty,
nothing to drain. Re-verified both standing escalations against current
code, no drift: `supabase/functions/openPosition/index.ts` still has zero
`recordAttempt` references; `supabase/functions/oauthDiag/index.ts` still
gated only by `requireUser` (line 31), no admin check. Re-verified the
`228fea9` gap: still not on `main` (`git merge-base --is-ancestor 228fea9
origin/main` fails), still no open PR for it (`list_pull_requests`
state=open on this repo returns only #5, `claude/growth-2026-W37` →
`staging`, unrelated) — still a `needs owner` item, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked list in `docs/ops/access.md`; not reported as
down.

**The checks.** Fresh container, `npm ci` first (611 packages, clean).
`npm run lint` clean. `npm test` — 193/193 passing. `npm run build`
clean. `npm run context:check` initially stale once `shipped.md` and
`queue.md` were edited this run — ran `npm run context` to regenerate
`docs/product-context.md`, then `context:check` passed clean.

**Fixed:** nothing — no new plain bug found, nothing in the `open` queue
to drain.

**Escalated:** none new — the `SUPABASE_SERVICE_ROLE_KEY_STAGING` gap is a
`needs owner` secret request, not a bug or a number-meaning change.

**No email sent.** The new finding is a missing CI secret already visible
in the commit's own message and the workflow's own refusal log, not a
money-path or user-visible defect; everything else this run is an
unchanged standing item.

## 17:08 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`.
`git fetch origin`, `git checkout staging` (clean, up to date with
`origin/staging`, at `3c69ce5`).

**What changed.** The last ledger entry (16:09 UTC) committed as `9d167d5`
(16:11:32 UTC); `git log --since="2026-09-07T16:11:32Z" --oneline main
staging` turned up seven new commits, all staging-only and all touching
`landing/` (or its adjacent `scripts/email-digest.mjs`): `1018978` and
`3c69ce5` (the digest email is now a card per report instead of the whole
report pasted in, plus three defects the new renderer's own dry run against
a real report surfaced: a stray unclosed asterisk survives clipping, a
subject that named the audit label twice, and a summary line pulled from
process metadata instead of the actual findings); `da39999` and `2029e30`
(content:check now requires two lists and no paragraph over 110 words per
post, a markdown-renderer defect that silently rendered a wrapped list item
as a paragraph with literal dashes is fixed, and the credit-spread post's
stale `draft: true` — set 3 September when the file was a 150-word stub —
is removed so the 2,100-word article the file has carried since actually
publishes); `333503a` and `4501bd0` (Blog, Pricing and Log in move behind a
phone menu button instead of being hidden below 640px, where they were
previously unreachable from the top of any page on a phone); `addf7c7`
(the homepage's looping Market Screener replay reserves its tallest
measured panel height instead of its shortest, so the sections below it no
longer shift while the animation plays). Added four grouped lines to
`docs/ops/shipped.md`, all tagged `(staging)` — `git merge-base
--is-ancestor <sha> origin/main` fails for all seven.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment
(checked without printing it) — `opsHealth` not called on either project,
same as every prior run. `docs/ops/queue.md` re-read: `open` section still
empty, nothing to drain. Re-verified both standing escalations against
current code, no drift: `supabase/functions/openPosition/index.ts` still
has zero `recordAttempt` references; `supabase/functions/oauthDiag/index.ts`
still gated only by `requireUser`, no admin check. Re-verified the
`228fea9` gap: still not on `main`, still a `needs owner` item, not
duplicated. Re-verified the `SUPABASE_SERVICE_ROLE_KEY_STAGING` gap from
the 16:09 run: still an open `needs owner` ticket, not duplicated.

**What is up.** All four hosts (`deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app`) — `CONNECT tunnel failed, response 403`,
matching the known-blocked list in `docs/ops/access.md`; not reported as
down.

**The checks.** Fresh container, `npm ci` first (611 packages, clean).
`npm run lint` clean. `npm test` — 193/193 passing (`landing/src/blog.test.js`
included in the suite, itself extended this run by `da39999` and already
green). `npm run build` clean. `npm run context:check` initially stale once
`shipped.md` was edited this run — ran `npm run context` to regenerate
`docs/product-context.md`, then `context:check` passed clean.

**Fixed:** nothing — no new plain bug found reading the seven commits above;
nothing in the `open` queue to drain.

**Escalated:** none new — no findings this run.

**No email sent.** Nothing on the money path or user-visible that isn't
already an unchanged standing `needs owner`/`escalated` item from a prior
run; this run's own findings are all documentation (`shipped.md`) of
already-shipped, already-tested staging content and UI work.
