# 2026-09-23

## 13:12 UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded cleanly
at `649814e`.

**What changed.** A lot happened overnight that no duty-engineer run had
ledgered yet, and `main` and `staging` both moved. The owner merged
`staging` into `main` at `01e0bcc` (second parent `e87c119`), so most of the
day's work now lives on both branches; `git log --oneline 7e7de08..origin/staging`
and `git log --oneline 280e9ce..origin/main` were both read in full to find
what was new. Added four lines to `docs/ops/shipped.md`, none of which had
one yet:

- The logo is now the lowercase Greek delta, drawn in the app's own
  wordmark font rather than the old triangle-and-sprigs mark, with its
  size/spacing/vertical centering derived from one number instead of four
  independent hand-set ones across the app header, drawer, auth and site
  nav/footer (`386841d`, `9b67a8e`, `507dc15`, `2858a90`; on `main` via the
  merge).
- A twelfth foundations post — options expiration — is live: exercise by
  exception, the two cutoffs, per-leg settlement, pin risk (`63ca49e`, on
  `main`).
- Unfinished broker work (SnapTrade/Tradier) no longer has to block small
  fixes from shipping: one source tree, one build flag (`VITE_LAB`) that
  staging turns on and production leaves off, with the unfinished code
  physically stubbed out of a production build rather than just hidden
  behind a check, and the two edge functions refusing on their own if
  called against an environment without the credentials the work needs.
  This replaced an earlier same-day attempt to move the work to its own
  branch, which the owner correctly called out as the wrong shape — kept
  for the record in the commit history (`4fc84f3`, reverted `c655223`,
  `e87c119`; on `main` via the merge).
- The blog's diagrams can reach the live site again — see "What broke"
  below; this is the same finding as the standing 2026-09-21 escalation,
  not a new one, so it is ledgered as shipped but not re-escalated
  (`649814e`, `staging` only so far).

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment;
opsHealth not called on either project — read the queue and code instead,
as the brief allows.

`docs/ops/queue.md` unchanged since the 2026-09-22 18:15 run (`7c48d0f`)
except for what I found reading it against today's commits: `649814e`
("Landing: put the published blog diagrams on staging, so the site can
deploy") is the root-cause fix for the standing 2026-09-21 blog-diagrams
escalation — not a duty-engineer fix, landed directly by a session working
with the owner on today's brand change. The actual mechanism, now
confirmed instead of proposed: `publish-blog.yml` commits a post's SVGs
straight to `main`; nothing ever mirrored them onto `staging`;
`deploy-landing.yml`'s staging-first gate compares `landing/` byte-for-byte
and has refused every deploy since 14 September as a result. So it was
never the diagrams that were broken, it was the deploy — and every landing
change since, including today's whole logo change, had been piling up
undeployed behind it with nothing louder than a quiet gate skip. The eight
images are now on `staging` too; `git diff --stat origin/main origin/staging
-- landing/` is empty as of this run, so the gate should pass on the next
deploy. **Still not the real fix** — the publishing pipeline still writes
only to `main`, so the same gap reopens on the next diagram post unless
that pipeline itself changes, which is still the owner's call. Updated the
existing queue entry in place (not a new ticket) and posted a status
comment on the still-open
[PR #11](https://github.com/OsamaMaghawry/Spreads/pull/11) — the owner is
clearly already aware (the fixing commit names the PR directly), so this is
a record update, not an attempt to reach him for the first time; no email
sent, since nothing here is new information he doesn't already have.

Checked [PR #9](https://github.com/OsamaMaghawry/Spreads/pull/9)
(`staging` → `main`, migration confirmation): head is the `staging` branch
itself, so it now carries every commit above automatically, including this
run's ledger commit. No new PR needed.

The systems-engineer/head-of-trading bench backlog under queue.md's `##
Open` section was skimmed for anything new — unchanged, still that bench's
own backlog, not duty-engineer's to drain.

**What is up.** `deltamint.app`, `/blog`, `/pricing`, `dashboard.deltamint.app`
re-fetched directly: `CONNECT tunnel failed, response 403` on all four,
same standing policy block already on record in `docs/context/reachable.md`.
Not reported as down; no new row needed.

**The checks**, on `staging` (`649814e`) after `npm ci` (`node_modules` not
present at session start): `npm run lint` clean; `npm test` — 407 client +
634 server, 1,041 pass, 0 fail (634 now includes the SnapTrade/Tradier
tests, restored by the revert); `npm run build` clean in both build modes
implicitly verified (`VITE_LAB` off locally matches production; the commit
history above records both modes tested green when the flag was added);
`npm run context:check` was red after the `shipped.md` additions above —
mechanical, ran `npm run context`, re-ran the full suite green.

**Drained.** Nothing new and untriaged beyond the blog-diagrams update
above — every other open item remains correctly filed as `needs owner` or
`escalated`, and the bench backlog is not duty-engineer's.

**Fixed this run:** nothing new by this run — no application bug found;
`docs/ops/shipped.md` caught up on four commits and `docs/product-context.md`
regenerated to match, `docs/ops/queue.md` updated to record the
blog-diagrams root cause, all pushed to `staging`.
**Escalated this run:** nothing new — updated the existing 2026-09-21
blog-diagrams escalation with today's fix and its remaining gap; commented
on PR #11 with the same update.
**Email:** not sent — the diagram-deploy finding is the owner's own fix,
already known to him; nothing else this run is new, money-path or
user-visible. Clean hour: ledger caught way up, checks green.

## 14:13 UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded cleanly,
landing on `32ae263`.

**What changed.** One new commit on `staging` since the 13:12 run:
`32ae263`, "Analysis: chart first, one tab at a time, method last (lab)" —
a reworked Analysis layout (behind `LAB_MODULES`, stubbed out of the
production bundle) that reorders the page to result-and-chart first, then
stat tiles, then one detail tab at a time, then method notes last, and
moves each compliance caveat onto the one figure it actually affects
instead of repeating it three times at the top. It touches `src/`, so a
line for it did not exist yet — added to `docs/ops/shipped.md`. `main`
gained only its own metrics-snapshot and blog-publish automation since the
last run's `01e0bcc`; nothing there needed a new line.

**What broke.** `DELTAMINT_OPS_TOKEN` is not set in this environment, so
the opsHealth endpoint could not be called on either project — noted, not
worked around. Read `docs/ops/queue.md` in full (`Needs owner`,
`Escalated`, `Open`, `Fixed` sections): nothing new or untriaged. The
`Open` section's items are all systems-engineer/head-of-trading bench
backlog from the 2026-09-08/09 leg-first audit — not duty-engineer's
tickets, per the same read every run this week.

**What is up.** `deltamint.app`, `/blog`, `/pricing`, `dashboard.deltamint.app`
re-fetched directly: `CONNECT tunnel failed, response 403` on all four,
same standing policy block on record in `docs/context/reachable.md`. Not
reported as down.

**The checks**, on `staging` after `npm ci` (`node_modules` not present at
session start): `npm run lint` clean; `npm test` — 407 client + 634
server, 1,041 pass, 0 fail; `npm run build` clean (`VITE_LAB` off, matching
production) and re-checked with `VITE_LAB=1` also clean; `npm run
context:check` was red — mechanical, caused by this run's own
`shipped.md` addition — ran `npm run context`, re-checked green.

**Drained.** Nothing new and untriaged — every open item remains correctly
filed as `needs owner`, `escalated`, or bench backlog that isn't
duty-engineer's.

**Fixed this run:** nothing new by this run — no application bug found;
`docs/ops/shipped.md` caught up on the one new commit and
`docs/product-context.md` regenerated to match.
**Escalated this run:** nothing new.
**Email:** not sent — nothing new, money-path or user-visible, surfaced
this run. Clean hour: one commit ledgered, checks green.

## 15:11 UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded
cleanly, landing on `30dbe06` (this branch's own 14:13 run commit).

**What changed.** No new commits on `staging` or `main` since the 14:13
run — `staging` HEAD is unchanged at `30dbe06`, and `git log --since="26
hours ago" --oneline main staging` shows nothing past that commit.
`docs/ops/shipped.md` is already caught up (top entry is the lab Analysis
layout, `32ae263`); nothing to add.

**What broke.** `DELTAMINT_OPS_TOKEN` is not set in this environment, so
the opsHealth endpoint could not be called on either project — noted, not
worked around. Read `docs/ops/queue.md` in full (`Needs owner`,
`Escalated`, `Open`, `Fixed` sections): nothing new or untriaged. Every
open item remains correctly filed as `needs owner`, `escalated`, or
systems-engineer/head-of-trading bench backlog that isn't duty-engineer's.

**What is up.** `deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app` re-fetched directly: `CONNECT tunnel failed,
response 403` on all four, same standing policy block already on record
in `docs/context/reachable.md`. Not reported as down.

**The checks**, on `staging` after `npm ci` (`node_modules` not present at
session start): `npm run lint` clean; `npm test` — 407 client + 634
server, 1,041 pass, 0 fail; `npm run build` clean; `npm run context:check`
clean, no regeneration needed.

**Drained.** Nothing new and untriaged — every open item remains correctly
filed.

**Fixed this run:** nothing — no new commit, no application bug found.
**Escalated this run:** nothing new.
**Email:** not sent — nothing new, money-path or user-visible, surfaced
this run. Clean hour: one ledger line, checks green.

## 16:1x UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded
cleanly, landing on `0dd4b41`.

**What changed.** Three new commits on `staging` since the 15:11 run
(`afdb128`), none by duty-engineer — a session working the covered-call
cover logic:
- `3964cb6` — the Scanner's covered-call suggestions now count a free long
  call as cover (not just free shares), and stop offering a second call
  against shares or a long that already backs an existing short call.
- `95a7b3f` — the option chain prices a call sold against a covering long
  call correctly instead of as naked, and the close ticket's payoff chart
  draws the long call's floor instead of showing unbounded loss above the
  strike on a position that is actually covered.
- `0dd4b41` — the Open Position screen now names a ticker its own scan
  loop skipped (matching the reason the Scanner already shows) and stops
  on "no free cover at all" instead of retrying silently forever.

All three touch `src/`; added one shipped.md line per commit (not there
yet). `main` unchanged since `01e0bcc` — nothing new to ledger there.

**What broke.** `DELTAMINT_OPS_TOKEN` still not set in this environment;
opsHealth not called on either project — read the queue and code instead,
as the brief allows. Read `docs/ops/queue.md` in full (`Needs owner`,
`Escalated`, `Open`, `Fixed`): unchanged since the 13:12 run's edit
(`afdb128`'s predecessor); nothing new or untriaged. The `Open` section
remains the systems-engineer/head-of-trading bench backlog, not
duty-engineer's, per every prior run's read this week.

**What is up.** `deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app` re-fetched directly: `CONNECT tunnel failed,
response 403` on all four, same standing policy block already on record
in `docs/context/reachable.md`. Not reported as down; no new row needed.

**The checks**, on `staging` (`0dd4b41`) after `npm ci` (`node_modules`
not present at session start): `npm run lint` clean; `npm test` — both
node --test invocations passed, 659 server-side tests all green (up from
634, three new commits' own test coverage) and the client/worker/blog
suite ahead of it exited 0; `npm run build` clean (`VITE_LAB` off,
matching production); `npm run context:check` was red — mechanical, from
this run's own `shipped.md` additions — ran `npm run context`, re-checked
green.

**Drained.** Nothing new and untriaged — every open item remains correctly
filed as `needs owner`, `escalated`, or bench backlog that isn't
duty-engineer's.

**Fixed this run:** nothing new by this run — no application bug found;
`docs/ops/shipped.md` caught up on the three new commits and
`docs/product-context.md` regenerated to match, pushed to `staging`.
**Escalated this run:** nothing new.
**Email:** not sent — nothing new, money-path or user-visible, surfaced
this run. Clean hour: ledger caught up, checks green.
