# 2026-09-24

## 13:13 UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded
cleanly, landing on `c5ea547`.

**What changed.** Thirteen new commits on `staging` since the last run
(`e8a4f1b`, 2026-09-23 20:11 UTC), all merged into `main` too — an Alpaca
compliance/branding push plus product changes the owner asked for directly.
None had a `docs/ops/shipped.md` line yet; added thirteen, newest first:
the Alpaca in-app authorization disclosure restored before the OAuth
redirect (`c6f07b8`); generated Terms/Privacy PDFs and a branding review,
with the homepage broker logo briefly swapped for text (`38679e2`); the
share image and legal PDFs made generated-from-source instead of hand-made
copies that go stale (`df68c09`); the Alpaca logo restored in the
integrations section (`7433d2e`); share-preview versioning so staging shows
its own card and previewers pick up changes (`abee00f`); the share card
simplified to logo + headline (`2435efe`); the share card redrawn to show
the actual product screens (`fa9d66b`); a pricing-schedule PDF for
compliance, not linked from the site (`c6e8df5`); a new About page and
Home/About in the site menu (`d9b71e6`); the free trial cut from 30 to 7
days, Terms updated for monthly/yearly billing (`8c918dc`); the branding
review also as a PDF, covering About (`7a3d2ba`); a Sign up button in the
menu and "Supported brokers" label on the homepage (`821fd62`); and the
homepage's phone view cut from four DeltaMint cards to two (`c5ea547`). Six
further commits on `main` only (`4ade3eb`, `26a7513`, `490b7fd`, `dacb16e`,
`d055553`, `cda8571`) are the daily content pipeline's post 13 (buying
power) plus a metrics snapshot — `content/` and `docs/growth/metrics/`
only, no `src/`, `supabase/functions/` or `landing/`, so nothing to ledger
per the brief.

**What broke.** `DELTAMINT_OPS_TOKEN` is not set in this environment, so
the opsHealth endpoint could not be called on either project — noted, not
worked around. Read `docs/ops/queue.md` in full (`Needs owner`,
`Escalated`, `Open`, `Fixed`): `git diff e8a4f1b -- docs/ops/queue.md` is
empty, confirming no change since the 2026-09-23 13:12 run (`ea3b2da`).
Nothing new or untriaged; the `Open` section remains the
systems-engineer/head-of-trading bench backlog from 2026-09-08/09, not
duty-engineer's, per every prior run.

**What is up.** `deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app` re-fetched directly: `CONNECT tunnel failed,
response 403` on all four, same standing policy block already on record in
`docs/context/reachable.md`. Not reported as down; no new row needed.

**The checks**, on `staging` (`c5ea547`) after `npm ci` (`node_modules` not
present at session start): `npm run lint` clean; `npm test` — 426 client +
663 server, 1,089 pass, 0 fail; `npm run build` clean (`VITE_LAB` off,
matching production) and re-checked with `VITE_LAB=1` also clean; `npm run
context:check` was red — mechanical, from this run's own `shipped.md`
additions — ran `npm run context`, re-checked green.

**Drained.** Nothing new and untriaged — every open item remains correctly
filed as `needs owner`, `escalated`, or bench backlog that isn't
duty-engineer's.

**Fixed this run:** nothing new by this run — no application bug found;
`docs/ops/shipped.md` caught up on the thirteen new `src/`/`landing/`
commits and `docs/product-context.md` regenerated to match, pushed to
`staging`.
**Escalated this run:** nothing new.
**Email:** not sent — nothing new, money-path or user-visible, surfaced
this run; all thirteen commits are the owner's own compliance/product work,
already known to him. Clean hour: ledger caught up, checks green.

## 14:12 UTC duty-engineer run

**Read.** `org.md`, `docs/ops/README.md`, `.claude/agents/duty-engineer.md`
per the brief. `git fetch origin && git checkout staging` succeeded
cleanly, landing on `8b4305e` — the previous run's own ledger commit.

**What changed.** `git log 8b4305e..HEAD` on `staging` is empty; nothing
landed on `staging` or `main` in the ~59 minutes since the 13:13 run.
`shipped.md` needs no new lines.

**What broke.** `DELTAMINT_OPS_TOKEN` is not set in this environment, so
the opsHealth endpoint could not be called on either project — noted, not
worked around. Read `docs/ops/queue.md` in full: `git diff e8a4f1b HEAD --
docs/ops/queue.md` is empty, confirming no change since 2026-09-23. Nothing
new or untriaged.

**What is up.** `deltamint.app`, `/blog`, `/pricing`,
`dashboard.deltamint.app` re-fetched directly: `EGRESS_BLOCKED` on all
four, same standing policy block already on record in
`docs/context/reachable.md`. Not reported as down; no new row needed.

**The checks**, on `staging` (`8b4305e`) after `npm ci`: `npm run lint`
clean; `npm test` — 426 client + 663 server, 1,089 pass, 0 fail; `npm run
build` clean; `npm run context:check` — `docs/product-context.md` is
current.

**Drained.** Nothing new and untriaged — every open item remains correctly
filed as `needs owner`, `escalated`, or bench backlog that isn't
duty-engineer's.

**Fixed this run:** nothing — no application bug found, nothing to fix.
**Escalated this run:** nothing new.
**Email:** not sent — nothing new, money-path or user-visible, surfaced
this run. Clean hour.
