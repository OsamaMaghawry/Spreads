# 2026-09-09

## 13:07 UTC run

**Read:** `docs/context/org.md`, `docs/ops/README.md`,
`.claude/agents/duty-engineer.md`, `docs/ops/queue.md`, `docs/ops/shipped.md`,
`docs/context/reachable.md`. `git fetch origin`, then `git checkout staging`
(clean checkout, created tracking `origin/staging` at `85c64ca` — 17 hours
since the last run, `c8add56` at 2026-09-08 20:06 UTC).

**What changed.** `git log --since="26 hours ago" --oneline main staging`
turned up 25 new commits since `c8add56`, none yet on `main`
(`git merge-base --is-ancestor <sha> origin/main` fails for all of them).
Twenty touched `src/`, `supabase/functions/` or `landing/`; five did not
(`609c3e1`, `c5c9950`, `af27f23`, `3b4a127`, `c79ab52` — content-plan/docs
only, out of scope). Of the twenty, five are engine-only work with no wired
behaviour change yet (`7ea6694`, `d2f59e8`, `2918301`, plus the shadow half
of `af75776`) and four are diagram/asset edits on still-unpublished blog
drafts (`0dac475`, `d4bfb5c`, `27b27cb`, `c4d1774`) — no shipped.md line for
either class, nothing a user can see changed. The remaining eleven commits
got seven `shipped.md` lines (grouping commits that land one user-facing
change together): a reprice-refusal fix (`85c64ca`), a blog title/byline
cleanup (`c72b562`, `dc92fac`), a new Broker tab with single- and
multi-select close (`4b523bc`, `91cc0f2`, `8f0e7fa`, `d520b22`), whole-market
scanning (`dcf0f17`), a missing-quote pricing fix (`2b9ccf7`, `3aee291`), a
production security fix on `staging` (`af75776` — see below), and a ratio
close-path fix (`619b0b7`, `1ee4e8f`).

**What broke.** `DELTAMINT_OPS_TOKEN` not set — `opsHealth` not called on
either project; read what I could from code and the queue instead.
`docs/ops/queue.md`: `Open` holds ~20 items, all dated 2026-09-09 and
authored by `systems-engineer`/`head-of-trading` on the in-flight leg-first
risk-engine rebuild, explicitly gated behind a re-audit "before any of this
goes to production" — design-level work already correctly owned by the
review chain, not duty-engineer's plain-bug authority; left untouched.
Re-verified all three standing findings against current code, no drift:
`tradeReconstruction.ts`'s `nearestLong` (line 558) still credit-side-only;
`openPosition/index.ts` still has zero `recordAttempt` references;
`oauthDiag/index.ts` still gated only by `requireUser`.

**New finding, escalated.** Reading `af75776`'s own description of what it
fixed on `staging` (`dumpBrokerFeed`'s missing ownership check) against
`origin/main`'s current `supabase/functions/dumpBrokerFeed/index.ts`: the
hole is still open in **production**. Any caller holding the app's public
anon key can name any account id and have its Alpaca credentials decrypted
and its full position/order/activity history fetched — no signed-in check,
no ownership check, on `main` right now. The fix exists and is tested
(`af75776`, 303 server tests green on `staging`) but has not been merged.
Written to `docs/ops/queue.md` under `Escalated` with the evidence and the
proposed action (merge/deploy the existing `staging` fix). Outside
duty-engineer's authority — touches credentials, and the fix is the owner's
own merge/deploy step, not a new one to write. Emailed the owner (see below).

**What is up.** Fetched all four hosts: `CONNECT tunnel failed, response
403` at the proxy for `deltamint.app`, `/blog`, `/pricing` and
`dashboard.deltamint.app` — same failure as every prior run, matches
`docs/context/reachable.md`; not reported as down.

**The checks.** Fresh container, `npm ci` (611 packages, clean). `npm run
lint` clean. `npm test` — 128 client + 345 server = 473 passing, all green
(up substantially from the 20:06 UTC run's count — the 25 intervening
commits added test files across most of them: `bookRisk.test.ts`,
`legMath.test.ts`, `structureName.test.ts`, `positionLegs.test.ts`,
`universe.test.ts`, `callCover.test.ts`, `resumeLimit.test.ts`,
`mlegLimits.test.ts`, `quoteSanity.test.ts` among others). `npm run build`
clean.
`npm run context:check` failed once after the `shipped.md`/`queue.md` edits
above (doc-affecting files changed); ran `npm run context` to regenerate
`docs/product-context.md`, then `context:check` passed clean.

**Fixed:** nothing this run — no open ticket inside duty-engineer's plain-bug
authority, no red check on staging.

**Escalated:** one, new — the production `dumpBrokerFeed` authorization hole
above.

**Email attempted, blocked, worked around.** Money-path finding (decrypted
broker credentials reachable by any anon-key holder, live in production
right now) needs the owner. Tried `sendDigest` per the brief's own recipe —
`curl` to `yecfbeohyakuoyczvdbj.supabase.co` failed with `CONNECT tunnel
failed, response 403`, the same block this environment already has on
`dashboard.deltamint.app`/`deltamint.app`. Not tested before; recorded in
`docs/context/reachable.md` and filed as a `needs owner` allowlist ticket.
Used `PushNotification` instead to reach the owner directly with the same
finding, since the alternative was letting a live production credential
hole go unreported this run.
